#!/usr/bin/env python3
"""Original reader-facing content for Chapter 02: sparsity and pruning."""

from __future__ import annotations

from typing import Any


COMMON_REFS = {
    "pytorch_pruning": ("PyTorch pruning tutorial", "https://docs.pytorch.org/tutorials/intermediate/pruning_tutorial.html"),
    "pytorch_profiler": ("PyTorch profiler documentation", "https://docs.pytorch.org/docs/stable/profiler.html"),
    "pytorch_benchmark": ("PyTorch benchmark utilities", "https://docs.pytorch.org/docs/stable/benchmark_utils.html"),
    "deep_compression": ("Deep Compression", "https://arxiv.org/abs/1510.00149"),
    "gradual_pruning": ("To Prune, or Not to Prune", "https://arxiv.org/abs/1710.01878"),
    "depgraph": ("DepGraph paper", "https://arxiv.org/abs/2301.12900"),
    "torch_pruning": ("Torch-Pruning reference implementation", "https://github.com/VainF/Torch-Pruning"),
    "network_slimming": ("Network Slimming", "https://arxiv.org/abs/1708.06519"),
    "taylor": ("Pruning CNNs for Resource Efficient Inference", "https://arxiv.org/abs/1611.06440"),
    "cusparselt": ("NVIDIA cuSPARSELt documentation", "https://docs.nvidia.com/cuda/cusparselt/"),
    "tensorrt_sparse": ("TensorRT sparsity requirements", "https://docs.nvidia.com/deeplearning/tensorrt/latest/inference-library/data-formats-tensors.html"),
    "tfmot": ("TensorFlow Model Optimization pruning guide", "https://www.tensorflow.org/model_optimization/guide/pruning"),
    "tf_strip": ("TensorFlow strip_pruning API", "https://www.tensorflow.org/model_optimization/api_docs/python/tfmot/sparsity/keras/strip_pruning"),
    "openvino": ("OpenVINO model optimization guide", "https://docs.openvino.ai/2023.3/openvino_docs_model_optimization_guide.html"),
    "nncf": ("NNCF reference implementation", "https://github.com/openvinotoolkit/nncf"),
    "onnx_checker": ("ONNX checker API", "https://onnx.ai/onnx/api/checker.html"),
    "onnx_shapes": ("ONNX shape inference", "https://onnx.ai/onnx/repo-docs/ShapeInference.html"),
    "resnet": ("Deep Residual Learning for Image Recognition", "https://arxiv.org/abs/1512.03385"),
    "coco": ("COCO evaluation", "https://cocodataset.org/#detection-eval"),
    "head_pruning": ("Are Sixteen Heads Really Better than One?", "https://arxiv.org/abs/1905.10650"),
    "sparsegpt": ("SparseGPT", "https://arxiv.org/abs/2301.00774"),
    "wanda": ("Wanda", "https://arxiv.org/abs/2306.11695"),
    "distillation": ("Distilling the Knowledge in a Neural Network", "https://arxiv.org/abs/1503.02531"),
    "determinism": ("PyTorch reproducibility notes", "https://docs.pytorch.org/docs/stable/notes/randomness.html"),
    "mlflow": ("MLflow tracking documentation", "https://mlflow.org/docs/latest/ml/tracking/"),
    "tflite": ("TensorFlow Lite model optimization", "https://www.tensorflow.org/lite/performance/model_optimization"),
}


def L(
    no: int,
    slug: str,
    title: str,
    puzzle: str,
    hook: str,
    objects: str,
    mechanism: str,
    anchors: list[str],
    checks: list[str],
    experiment: str,
    baseline: str,
    candidate: str,
    controlled: str,
    metrics: str,
    code_walk: str,
    gate: str,
    failure: str,
    next_step: str,
    conclusion: str,
    evidence_label: str,
    result_fields: list[tuple[str, str, str]],
    refs: list[str],
) -> dict[str, Any]:
    return locals()


LESSONS: list[dict[str, Any]] = [
    L(
        1,
        "pruning-objectives",
        "Pruning Objectives, Constraints, and Delivery Boundaries",
        "If half the weights become zero, has a mobile deployment objective been achieved?",
        "Pruning is an engineering change with several possible objectives: package size, resident memory, first-token or first-frame latency, steady-state throughput, energy, and hardware cost. A sparsity percentage answers none of them by itself. The first deliverable is therefore a target card that connects one model revision and workload to a measurable deployment gate and a rollback condition.",
        "The concrete objects are a dense linear layer, a same-shape masked layer, a physically narrower layer, their parameter tensors, the runtime input shape, and a latency distribution. The mask changes values; the narrower layer changes dimensions and the amount of dense work presented to the library.",
        "For a dense matrix multiplication, leading work is approximately `2MKN`. Replacing half of W with zeros leaves M, N, and K unchanged when the operator still dispatches a dense GEMM. Physically reducing the output width changes N and therefore both arithmetic and output storage. A valid target card distinguishes logical sparsity, serialized representation, physical shape, kernel path, and end-to-end metric. This is why a parameter-count goal and an 80 ms first-frame SLO are related but not interchangeable.",
        [
            "Logical zeros do not imply fewer dense instructions.",
            "A physical dimension change is visible to both the graph and the runtime.",
            "The acceptance metric must match the deployment objective and workload.",
        ],
        [
            "Predict whether a masked 50% sparse matrix will materially beat its dense copy in ordinary dense GEMM.",
            "Predict how the narrower layer changes parameters, FLOPs, and output shape.",
            "Write one acceptance gate and one rollback gate for a latency-driven project.",
        ],
        "Compare dense, same-shape masked, and physically narrower CUDA linear layers under one timing protocol.",
        "dense BF16 linear layer at the original shape",
        "50% same-shape masking and a 50% physically narrower dense layer",
        "GPU, input batch, input width, dtype, warm-up, repetitions, and random seed",
        "logical sparsity, physical parameters, median/p95 latency, and output width",
        "The lab constructs all three candidates from one base weight tensor. The masked candidate preserves the dense shape, while the narrow candidate copies a selected subset of rows. CUDA events bracket only repeated forward calls after warm-up. Reading these rows together shows which optimization changed values and which changed the work exposed to the runtime.",
        "Accept a pruning route only if the deployment metric improves under the frozen workload and the quality gate passes; otherwise keep the dense revision as the explicit rollback.",
        "A narrow microbenchmark can still mislead if first-frame setup, preprocessing, memory allocation, or a mobile runtime dominates. Conversely, masking may compress well on disk even when it does not accelerate the measured dense operator. Never transfer one objective's success to another objective without new evidence.",
        "Add model serialization and a real deployment runtime, then repeat cold-start and steady-state tests separately. Record the graph dimensions and operator trace beside the latency table.",
        "Zero weights satisfy a sparsity statistic; only a supported representation and a measured deployment path satisfy a performance objective.",
        "pytorch-gpu",
        [("Dense parameters", "dense_parameters", "int"), ("Masked logical sparsity", "masked_sparsity", "percent"), ("Narrow parameters", "narrow_parameters", "int"), ("Dense median", "dense_median_ms", "ms"), ("Masked median", "masked_median_ms", "ms"), ("Narrow median", "narrow_median_ms", "ms")],
        ["pytorch_profiler", "deep_compression"],
    ),
    L(
        2,
        "sparsity-granularity",
        "The Sparsity Granularity Spectrum: Weights, Channels, Blocks, and N:M",
        "Can two tensors with exactly 50% zeros demand different kernels and deployment formats?",
        "The word sparsity hides a layout contract. Unstructured zeros, contiguous blocks, removed channels, and 2:4 groups can share the same global nonzero rate while exposing very different metadata, vectorization, and library opportunities. Choosing a granularity is therefore a joint algorithm-runtime decision, not a cosmetic choice made after training.",
        "The lab uses one weight matrix and derives four representations: unstructured magnitude zeros, block zeros, exact 2:4 groups, and a physically narrowed matrix. It tracks global sparsity, 2:4 compliance, shape, and dense-path latency.",
        "A global rate `1 - nnz/numel` discards where the nonzeros live. For 2:4 sparsity, every consecutive group of four along the contracted dimension must contain exactly two retained values; 50% zeros placed elsewhere are non-compliant. Block sparsity adds a block shape and index structure. Channel pruning removes a complete axis and can reuse dense kernels at a smaller dimension. Runtime value comes from matching one of these contracts to an implementation.",
        [
            "Equal nonzero counts do not imply equal layouts.",
            "N:M compliance is a local invariant, not a global percentage.",
            "Channel removal can use a smaller dense operator without sparse metadata.",
        ],
        [
            "Predict which 50% mask will pass an exact 2:4 compliance check.",
            "Predict whether ordinary dense matmul notices unstructured or block zeros.",
            "Choose a granularity when custom kernels are forbidden.",
        ],
        "Construct four 50%-budget representations and compare compliance, shape, and ordinary dense CUDA timing.",
        "original dense matrix and unstructured 50% magnitude mask",
        "block mask, exact 2:4 mask, and a physically narrowed dense matrix",
        "source weights, input batch, dtype, target zero budget, and timing method",
        "global sparsity, 2:4 compliance, physical shape, and median latency",
        "Each mask is generated explicitly so its local structure can be inspected. The experiment intentionally multiplies masked tensors through the ordinary dense PyTorch path; it does not claim cuSPARSELt dispatch. The narrow candidate changes the contracted work and provides a useful control for the claim that structure, not zero count, is what the kernel sees.",
        "Select a granularity only after the target runtime's supported patterns and the model's accuracy sensitivity are both written down.",
        "A 2:4-compliant tensor can still use a dense tactic when it is not compressed into the required backend format, dtype, alignment, or build flag. A channel-pruned tensor can also be slower at awkward widths. Compliance is necessary for some paths, never sufficient for speed.",
        "Run the compliant matrix through cuSPARSELt or TensorRT, capture its tactic log, and sweep dimensions around alignment boundaries while holding the nonzero budget fixed.",
        "Sparsity granularity is an interface between optimization and execution; a global zero rate is only one field of that interface.",
        "pytorch-gpu",
        [("Unstructured sparsity", "unstructured_sparsity", "percent"), ("2:4 compliance", "nm_compliance", "percent"), ("Dense median", "dense_median_ms", "ms"), ("Unstructured median", "unstructured_median_ms", "ms"), ("2:4 dense-path median", "nm_dense_path_median_ms", "ms"), ("Narrow median", "narrow_median_ms", "ms")],
        ["cusparselt", "pytorch_pruning"],
    ),
    L(
        3,
        "baseline-measurement",
        "Baseline Measurement: Parameters, FLOPs, Latency, and Throughput",
        "Which baseline numbers are required before a pruning result can be interpreted?",
        "A pruning percentage without a baseline is not a comparison. Parameters and analytical FLOPs describe model structure; median and tail latency describe a workload on one stack; throughput and peak memory answer still different questions. A useful baseline freezes all of them before changing the model.",
        "The concrete system is a three-layer CUDA MLP, two batch sizes, a known dtype, a fixed random input, a parameter counter, an analytical linear-FLOP ledger, repeated CUDA-event samples, and peak allocated memory.",
        "For linear layers, parameters are `in_features × out_features` plus bias and leading multiply-add work is `2 × batch × in × out`. These values are deterministic properties of the chosen shape. Latency is a distribution affected by warm-up, synchronization, and batch; throughput is `batch / elapsed_time` and cannot be inferred from a single-request timing. Peak allocated memory must be reset and sampled over the same measurement window.",
        [
            "Structural metrics are deterministic for a frozen graph.",
            "Latency and throughput depend on the workload and timing protocol.",
            "Tail statistics require repeated samples, not one synchronized call.",
        ],
        [
            "Predict how batch 1 and batch 64 change latency and examples per second.",
            "Explain why lower FLOPs does not mathematically guarantee lower p95 latency.",
            "List every environment field required to compare a later pruned run.",
        ],
        "Record a complete dense MLP baseline at batch 1 and batch 64 with structural and runtime metrics.",
        "the same dense MLP evaluated at batch 1",
        "the same dense MLP evaluated at batch 64",
        "model weights, hidden sizes, dtype, GPU, warm-up, repetitions, and input distribution",
        "parameters, analytical FLOPs, median/p95 latency, throughput, and peak allocated memory",
        "The notebook computes the structural ledger directly from module shapes, then uses the same timing helper for both batches. CUDA synchronization happens after the event pair, and the result retains every sample so p95 can be recomputed. The batch comparison is not a candidate victory; it demonstrates why service workload belongs in the baseline identity.",
        "Reject any pruning comparison that cannot reproduce the dense baseline within a predefined tolerance on the same hardware and software stack.",
        "Timing before warm-up can include allocator and kernel initialization. Dividing batch by host wall time without synchronization can overstate throughput. Peak memory from an earlier operation can contaminate the window. A baseline report should make each of these failure modes auditable.",
        "Add power, cold-start, and operator traces, then repeat across a batch/sequence grid. Use confidence intervals or repeated runs when the acceptance margin is close to noise.",
        "Parameters, FLOPs, latency, throughput, and memory are complementary baseline fields, not interchangeable compression scores.",
        "pytorch-gpu",
        [("Parameters", "parameters", "int"), ("Batch-1 FLOPs", "batch1_flops", "int"), ("Batch-1 median", "batch1_median_ms", "ms"), ("Batch-1 p95", "batch1_p95_ms", "ms"), ("Batch-64 median", "batch64_median_ms", "ms"), ("Batch-64 throughput", "batch64_examples_s", "rate"), ("Peak memory", "peak_memory_mib", "mib")],
        ["pytorch_profiler", "pytorch_benchmark"],
    ),
    L(
        4,
        "prune-finetune-loop",
        "Closing the Loop: Train, Prune, Recover, and Re-evaluate",
        "Why does a one-shot 70% mask often fail when the same target reached gradually can recover?",
        "Pruning changes an optimization problem, not just a checkpoint file. The model must absorb a perturbation while the remaining weights adapt. A closed loop records the dense state, pruning event, recovery budget, best recovered metric, final mask, and rollback decision instead of reporting only the target sparsity.",
        "A synthetic but separable classification dataset, a small MLP, one dense initialization, one-shot and staged pruning schedules, optimizer steps, masks reapplied after every update, and held-out accuracy form the experiment.",
        "Magnitude pruning projects weights onto a sparse support. An abrupt 70% projection can remove several co-adapted paths at once and move the loss far from the local basin. A staged schedule introduces smaller support changes followed by recovery. Because optimizers can regrow masked weights, the mask must be enforced after updates unless the parameterization guarantees zeros. Fairness requires both routes to begin from the identical dense checkpoint and consume a declared recovery budget.",
        [
            "Pruning is a state transition followed by constrained optimization.",
            "Masks must remain enforced during recovery.",
            "One-shot and gradual routes need equal initialization and declared budgets.",
        ],
        [
            "Predict the immediate accuracy drop after one-shot 70% pruning.",
            "Predict whether staged pruning will finish with a smaller or larger recovery gap.",
            "Name the artifact needed to prove that zeros did not regrow.",
        ],
        "Train one dense toy classifier, then compare one-shot and staged 70% pruning with equal recovery steps.",
        "one-shot 70% magnitude pruning from the frozen dense checkpoint",
        "three-stage pruning to the same target with interleaved recovery",
        "initial checkpoint, dataset, split, optimizer family, total recovery steps, seed, and final sparsity",
        "dense accuracy, immediate drop, recovered accuracy, final sparsity, and best recovery step",
        "The lab trains one baseline and clones it before either pruning route. A mask helper chooses the global threshold and a recovery helper reapplies the mask after every optimizer step. Both routes consume the same total number of updates; the staged route only changes when support is removed. This makes schedule the independent variable.",
        "Accept the pruned checkpoint only when held-out accuracy and sparsity both pass and the exact dense checkpoint remains available for rollback.",
        "A toy separable dataset can favor either schedule and does not predict ImageNet recovery. Comparing unequal training steps, learning rates, or data order also invalidates the causal claim. The lab establishes the control-loop mechanics, not a universal schedule ranking.",
        "Repeat with several seeds and recovery budgets, plot accuracy immediately before and after each pruning event, and add a distillation term as a separately controlled intervention.",
        "A pruning result is a trajectory with a support constraint and recovery budget, not a mask applied once.",
        "pytorch-gpu",
        [("Dense accuracy", "dense_accuracy", "percent"), ("One-shot immediate", "oneshot_immediate_accuracy", "percent"), ("One-shot recovered", "oneshot_recovered_accuracy", "percent"), ("Gradual recovered", "gradual_recovered_accuracy", "percent"), ("Final sparsity", "final_sparsity", "percent")],
        ["gradual_pruning", "deep_compression"],
    ),
    L(
        5,
        "unstructured-magnitude-pruning",
        "Unstructured Magnitude Pruning Without Storage Myths",
        "Why can a model contain 80% zeros while its ordinary state_dict grows?",
        "Unstructured magnitude pruning is easy to apply and useful for studying redundancy, but PyTorch's training-time reparameterization stores the original parameter and a mask. Logical zeros, raw checkpoint bytes, compressed bytes, and physical sparse storage are four distinct quantities.",
        "The experiment uses one linear module before pruning, after `l1_unstructured`, after `prune.remove`, and after gzip compression. It inspects parameter names, buffers, zero rate, forward equivalence, and serialized byte counts.",
        "PyTorch pruning replaces `weight` with `weight_orig` and computes `weight_orig × weight_mask` through a pre-hook. The dense tensors still occupy dense storage, and the additional mask can make an uncompressed state_dict larger. `prune.remove` materializes the masked weight and deletes the reparameterization but does not convert it to CSR or pack nonzeros. General-purpose compression can exploit repeated zero bytes, which explains why compressed file size may fall while raw tensor storage does not.",
        [
            "A mask changes parameterization before it changes storage format.",
            "Removing the reparameterization materializes zeros but keeps a dense tensor.",
            "Raw and compressed checkpoint sizes answer different deployment questions.",
        ],
        [
            "Predict the state_dict keys immediately after PyTorch pruning.",
            "Predict whether the raw serialized bytes shrink after `prune.remove`.",
            "Predict which representation gzip compresses most effectively.",
        ],
        "Trace an 80% magnitude mask through apply, save, remove, and compressed serialization stages.",
        "the original dense linear layer state_dict",
        "PyTorch pruning reparameterization and the materialized masked weight",
        "same weight values, pruning amount, serializer, compression level, and module shape",
        "zero rate, state_dict keys, raw bytes, gzip bytes, and forward equivalence",
        "BytesIO keeps the serialization experiment inside memory and avoids path-dependent artifacts. The notebook records keys before and after `remove`, evaluates the module around the transition, and compresses the same byte payload. It does not call the resulting file a sparse runtime format.",
        "Accept the mask lifecycle only when the saved keys, zero rate, load path, and intended deployment representation are explicitly tested.",
        "File systems and zip serialization can introduce version-dependent overhead, so tiny tensors exaggerate metadata. Gzip size is not resident GPU memory and says nothing about kernel speed. A deployment claiming sparse storage must identify the actual sparse encoding and loader.",
        "Convert the materialized matrix to CSR and compare metadata plus supported operations; then load every saved variant into a fresh process and verify outputs before benchmarking.",
        "Magnitude pruning creates zeros; storage compression and runtime acceleration require additional explicit representations.",
        "pytorch-gpu",
        [("Logical sparsity", "sparsity", "percent"), ("Dense raw bytes", "dense_raw_bytes", "bytes"), ("Pruned-hook raw bytes", "hook_raw_bytes", "bytes"), ("Removed raw bytes", "removed_raw_bytes", "bytes"), ("Removed gzip bytes", "removed_gzip_bytes", "bytes"), ("Remove max output drift", "remove_max_error", "float")],
        ["pytorch_pruning", "deep_compression"],
    ),
    L(
        6,
        "global-layerwise-budgets",
        "Global Sparsity and Layer-wise Budget Allocation",
        "Should a fixed 50% global budget prune every layer by 50%?",
        "Layers transform different signals and have different redundancy. A global threshold spends zeros where weights are small, while a uniform per-layer target ignores sensitivity. A budget table should preserve the total constraint and show why protected or aggressive allocations were assigned.",
        "A three-layer MLP, calibration inputs, one global magnitude mask, a uniform 50% mask, and a sensitivity-aware allocation are compared at equal total nonzero count using held-out output reconstruction.",
        "For network output `f(x; W)`, a layer's pruning cost depends on downstream amplification and the input distribution, not only its weight histogram. A first-order sensitivity sweep can mask a small fraction in one layer at a time and measure output change. Budgets can then be allocated inversely to observed sensitivity while solving the global nonzero constraint. The experiment keeps total zeros equal so quality differences come from allocation rather than extra capacity.",
        [
            "Global sparsity is a constraint across tensors, not a uniform policy.",
            "Layer sensitivity must be measured on representative inputs.",
            "Budget comparisons require equal total nonzeros.",
        ],
        [
            "Predict which layer will be protected by the calibration sweep.",
            "Predict whether uniform and global magnitude masks use the same per-layer rates.",
            "Name the quality metric and total-budget invariant required for fairness.",
        ],
        "Compare uniform, global-magnitude, and sensitivity-aware masks at the same 50% global zero budget.",
        "uniform 50% magnitude pruning in every layer",
        "global thresholding and sensitivity-aware per-layer allocation",
        "dense weights, calibration/held-out tensors, global zero count, dtype, and seed",
        "per-layer sparsity, total sparsity, held-out RMSE, cosine similarity, and calibration sensitivity",
        "The notebook first perturbs each layer separately to obtain a small calibration sensitivity score. It then constructs three cloned models and checks exact total sparsity before measuring held-out output error. The allocation heuristic is intentionally simple; the evidence target is the budget principle, not a claim of optimal pruning.",
        "Accept a layer budget only when the total constraint is exact and the ranking remains stable on held-out data or multiple calibration slices.",
        "Using the held-out set to allocate budgets leaks evaluation. Very small calibration batches make sensitivity noisy, and equal zero counts do not ensure equal metadata or runtime cost across layers. Hardware-aware costs may need a different budget unit than parameters.",
        "Repeat the sweep across domains, optimize budgets in latency-weighted channel units, and test whether protected early layers remain protected after recovery training.",
        "A global target needs a measured allocation rule; uniform layer rates are merely one candidate.",
        "numerical-model",
        [("Uniform RMSE", "uniform_rmse", "float"), ("Global RMSE", "global_rmse", "float"), ("Aware RMSE", "aware_rmse", "float"), ("Total sparsity", "total_sparsity", "percent"), ("Most sensitive layer", "most_sensitive_layer", "str")],
        ["gradual_pruning", "pytorch_pruning"],
    ),
    L(
        7,
        "filter-pruning",
        "Filter Pruning: Making Convolution Physically Narrower",
        "Why does zeroing filters differ from deleting them?",
        "A dense convolution library receives input/output channel counts, kernel size, stride, and dtype. Setting complete filters to zero preserves those dimensions. Physical filter pruning constructs a smaller convolution and propagates the selected channels to the next layer, allowing ordinary dense kernels to execute less work.",
        "Two consecutive convolutions form the concrete dependency. One candidate masks half of the first layer's output filters; another physically copies the retained filters and the matching input-channel slices of the second convolution.",
        "For a convolution, leading work scales with `N × Hout × Wout × Cout × Cin × Kh × Kw`. A zeroed filter leaves Cout unchanged in the operator descriptor. Deleting it halves the first layer's Cout and the next layer's Cin when dependencies are propagated. Copying the same retained weights provides an equivalence check between masked and narrowed functions before timing.",
        [
            "Filter zeros preserve the dense convolution descriptor.",
            "Physical pruning changes two coupled channel dimensions.",
            "Equivalence should be checked before a latency claim.",
        ],
        [
            "Predict the output shapes of masked and physically pruned blocks.",
            "Predict which candidate reduces analytical convolution work.",
            "Identify the exact slice that must be applied to the second convolution.",
        ],
        "Mask and physically remove the same convolution filters, then compare equivalence, parameters, FLOPs, and CUDA latency.",
        "same-shape filter-masked two-convolution block",
        "physically narrowed block with propagated second-layer input channels",
        "input tensor, retained filter indices, copied weights, batch, spatial shape, dtype, and timing",
        "output max error, parameters, analytical FLOPs, median latency, and channel shapes",
        "The physical model is rebuilt with smaller module dimensions and receives exact weight slices from the masked model. Summing the retained first-layer channels into the second layer is not approximated: the matching input-channel axis is sliced. Near-zero output drift verifies the dependency before speed and parameter numbers are interpreted.",
        "Accept structural filter pruning only when all consumer shapes are updated, functional drift is understood, and the target runtime improves under representative spatial sizes.",
        "Selecting channels independently in adjacent convolutions breaks equivalence. BatchNorm, residual adds, groups, and concatenations introduce additional dependencies not present in this two-layer probe. Awkward channel counts can also reduce kernel efficiency despite lower FLOPs.",
        "Insert BatchNorm and a residual branch, then use a dependency graph to enumerate every coupled slice. Sweep retained widths that align and misalign with the target convolution backend.",
        "Filter pruning accelerates ordinary dense convolution only after channel dimensions are physically rebuilt and propagated.",
        "pytorch-gpu",
        [("Masked parameters", "masked_parameters", "int"), ("Narrow parameters", "narrow_parameters", "int"), ("FLOP reduction", "flop_reduction", "percent"), ("Equivalence max error", "max_error", "float"), ("Masked median", "masked_median_ms", "ms"), ("Narrow median", "narrow_median_ms", "ms")],
        ["depgraph", "torch_pruning"],
    ),
    L(
        8,
        "network-slimming",
        "BatchNorm Scale Factors and Network Slimming",
        "When does a small BatchNorm gamma become a removable channel rather than merely a small multiplier?",
        "Network Slimming creates a train-time ranking signal by regularizing BatchNorm scale factors. The scale does not remove a channel by itself. Deployment still requires selecting indices, rebuilding the producing convolution, slicing BatchNorm state, and propagating the same indices into every consumer.",
        "A Conv-BN-ReLU-Conv block supplies convolution filters, BatchNorm gamma/beta/running statistics, retained channel indices, a gamma-masked control, and a physically narrowed copy.",
        "BatchNorm output per channel is `y_c = gamma_c (x_c - mu_c)/sqrt(var_c+eps) + beta_c`. A small gamma suppresses normalized variation, but beta can still contribute a constant and downstream weights can amplify it. Ranking by `|gamma|` is therefore a pruning heuristic learned under a sparsity regularizer. Physical removal is valid only when the chosen channel and all coupled parameters are sliced consistently and the resulting function is evaluated.",
        [
            "Gamma is an importance signal, not a structural deletion.",
            "BatchNorm affine and running-state tensors share the channel axis.",
            "Consumer weights must receive the identical retained indices.",
        ],
        [
            "Predict whether simply zeroing gamma matches physical deletion when beta is nonzero.",
            "List every BatchNorm tensor that must be sliced.",
            "Predict the output drift between a properly masked control and narrowed model.",
        ],
        "Rank channels by gamma, create a semantics-preserving masked control, and rebuild the block at half width.",
        "gamma-masked full-width Conv-BN-ReLU-Conv block",
        "physically narrowed block using the same retained gamma-ranked channels",
        "input, retained indices, all copied Conv/BN parameters, eval mode, dtype, and timing protocol",
        "gamma threshold, retained channels, output max error, parameters, and median latency",
        "The notebook sets the removed channels to a neutral post-BN value in the control before copying the retained convolution filters, BN state, and second-layer input slices. Eval mode freezes running statistics. The equivalence check isolates structural bookkeeping from the separate question of whether gamma ranking preserves task quality.",
        "Accept the ranking only after held-out quality, coupled slicing, physical width, and runtime evidence all pass.",
        "Small gamma values can be scale-invariant with neighboring weights, and nonzero beta breaks naive zero-gamma reasoning. Training without the intended L1 pressure may produce an uninformative ranking. Residual and concatenation consumers need a dependency graph beyond this local block.",
        "Train gamma with an explicit sparsity penalty, compare rankings across seeds, and propagate selected channels through a residual model with a graph-level pruning tool.",
        "Network Slimming turns BatchNorm scales into a ranking mechanism; deployment benefit begins only after consistent structural removal.",
        "numerical-model",
        [("Retained channels", "retained_channels", "int"), ("Gamma threshold", "gamma_threshold", "float"), ("Output max error", "max_error", "float"), ("Full parameters", "full_parameters", "int"), ("Narrow parameters", "narrow_parameters", "int"), ("Narrow median", "narrow_median_ms", "ms")],
        ["network_slimming", "depgraph"],
    ),
    L(
        9,
        "dependency-graph-pruning",
        "Residual, Concat, and Dependency-Graph Pruning",
        "Which tensors must change together when one residual branch loses channels?",
        "Structural pruning becomes a graph problem at merges. Addition requires shape equality; concatenation changes downstream channel offsets; normalization and projections carry the same channel semantics. A local low-importance decision therefore expands into a coupled pruning group.",
        "A two-branch residual block with Conv-BN paths, an addition, and a consumer convolution is used. The lab records the failure from pruning one branch alone, then constructs a synchronized narrower group.",
        "For `z = f(x) + g(x)`, both branch outputs must have identical shapes. Removing output indices I from f requires a compatible transformation in g and changes the consumer's input dimension. With concatenation, the retained index mapping is an offset union rather than equality. Dependency graphs encode these propagation rules so one root operation yields a complete group and can be rejected before it removes every channel.",
        [
            "Merge semantics determine dependency rules.",
            "A root channel decision propagates through producers, normalization, and consumers.",
            "A valid group must be checked for shape and over-pruning before mutation.",
        ],
        [
            "Predict the exception produced by pruning only one additive branch.",
            "Enumerate the tensors coupled to one output-channel deletion.",
            "Explain how concat propagation differs from addition.",
        ],
        "Trigger and capture an unsynchronized residual-shape failure, then build a synchronized narrow residual block.",
        "an invalid one-branch channel deletion caught as a diagnostic",
        "a coupled deletion across both branches, BatchNorm state, and the consumer",
        "source block, retained indices, input, eval mode, dtype, and copied parameters",
        "captured mismatch, synchronized output shape, output drift, parameters, and latency",
        "The invalid path is wrapped in a try/except so the notebook remains successfully executed while preserving the error message as evidence. The valid path rebuilds both branches with the same retained indices and slices the consumer input channels. This is a manual miniature of a dependency group.",
        "Accept a structural mutation only when a graph-level forward check, group-size guard, and downstream shape audit pass.",
        "Matching shapes does not prove semantic correctness: different branches may require coordinated importance scores, grouped-convolution divisibility, or static attribute updates. Dynamic control flow can also escape a trace-based dependency graph.",
        "Install Torch-Pruning, print the group details for an equivalent block, compare them with the manual ledger, and add a concat branch to test offset mappings.",
        "Structural pruning at graph merges is a coupled group operation, never an isolated tensor slice.",
        "pytorch-gpu",
        [("Mismatch captured", "mismatch_captured", "bool"), ("Retained channels", "retained_channels", "int"), ("Valid output channels", "valid_output_channels", "int"), ("Valid max error", "valid_max_error", "float"), ("Full parameters", "full_parameters", "int"), ("Narrow parameters", "narrow_parameters", "int")],
        ["depgraph", "torch_pruning"],
    ),
    L(
        10,
        "taylor-importance",
        "Taylor Importance: Ranking Channels by Loss Change",
        "Can a small-norm channel still have a large effect on the loss?",
        "Magnitude sees the parameter but not the data or objective. Taylor pruning uses the local product of an activation and its loss gradient to estimate how much removing a channel changes the loss. The approximation is cheap enough to rank many structures without a full retraining run for each one.",
        "A small classifier exposes one hidden activation tensor, its retained gradient, per-channel L1 weight scores, Taylor scores, and actual held-out loss increases from channel ablation.",
        "If channel activation `h_c` is replaced by zero, first-order expansion gives `Delta L_c ≈ |∂L/∂h_c · (-h_c)|`, aggregated across samples and positions. Weight L1 instead ranks `sum |W_c|`. Taylor incorporates the current data and loss but remains local: interactions between channels and higher-order curvature are omitted. Correlation with actual one-channel ablation is the direct diagnostic for this toy problem.",
        [
            "Taylor scores are objective- and data-dependent.",
            "Ablation loss change is the validation target for an importance ranking.",
            "First-order scores ignore interactions and distribution shift.",
        ],
        [
            "Predict whether L1 and Taylor produce identical rankings.",
            "Write the first-order term for zeroing one activation channel.",
            "Choose the correlation that validates each ranking against actual ablations.",
        ],
        "Compare L1 and Taylor channel rankings with exhaustive one-channel loss ablations on a held-out batch.",
        "channel ranking by outgoing/associated weight L1 magnitude",
        "channel ranking by absolute activation-gradient product",
        "trained toy classifier, calibration batch, held-out ablation batch, channel set, and loss",
        "Spearman correlation with actual loss increase, top-ranked channel, and loss deltas",
        "The notebook retains gradients on the hidden activation, performs one calibration backward pass, and aggregates `|h × grad|` per channel. It then runs controlled ablations on held-out inputs to construct the target ranking. The comparison measures ranking agreement rather than claiming a production pruning algorithm.",
        "Accept an importance metric only if its ranking is stable across representative batches and improves the declared quality-cost objective after pruning.",
        "One calibration batch can reverse scores, negative and positive first-order terms can cancel depending on aggregation, and simultaneous removal invalidates independent-channel estimates. Correlation on a tiny network does not establish ImageNet behavior.",
        "Repeat across batches, compare signed, absolute, and second-order approximations, then prune several channels jointly and measure how ranking quality degrades with sparsity.",
        "Taylor importance estimates local loss sensitivity; its value is established by held-out ablation agreement, not by the formula alone.",
        "numerical-model",
        [("L1 Spearman", "l1_spearman", "float"), ("Taylor Spearman", "taylor_spearman", "float"), ("L1 top channel", "l1_top_channel", "int"), ("Taylor top channel", "taylor_top_channel", "int"), ("Actual top channel", "actual_top_channel", "int"), ("Baseline loss", "baseline_loss", "float")],
        ["taylor", "pytorch_pruning"],
    ),
    L(
        11,
        "gradual-pruning-schedule",
        "Gradual Pruning Schedules and Recovery Training",
        "What does a polynomial sparsity schedule control that a final target does not?",
        "A target sparsity says where training should end; a schedule says how abruptly the feasible parameter set changes. With a fixed 15% recovery budget, pruning events compete with optimization steps, so the begin step, end step, update frequency, and learning-rate trajectory become part of the result.",
        "The experiment uses one frozen dense classifier, an immediate target mask, a polynomial target function, periodic magnitude-mask updates, identical recovery updates, and a held-out accuracy trajectory.",
        "A common cubic schedule is `s(t)=s_f+(s_i-s_f)(1-(t-t0)/(t1-t0))^3` within the pruning window. Early updates remove few weights and later changes taper as the target is approached. The schedule does not guarantee recovery; it bounds the size and timing of support shocks. Reapplying a newly ranked mask can remove previously useful weights, while a fixed mask only trains survivors. Those policy choices must be frozen.",
        [
            "Final sparsity does not identify the support trajectory.",
            "Pruning frequency trades adaptation time against ranking refresh.",
            "Learning-rate and sparsity schedules interact.",
        ],
        [
            "Compute the target sparsity halfway through a cubic schedule.",
            "Predict which route has the largest immediate loss shock.",
            "Name the schedule fields needed to reproduce a recovery trajectory.",
        ],
        "Compare one-shot and cubic gradual schedules under one initialization and equal optimizer-step budget.",
        "one-shot jump to 80% sparsity at the first recovery step",
        "cubic updates from 0% to 80% over the same recovery window",
        "dense checkpoint, data order, optimizer, learning rate, total steps, target rate, and mask rule",
        "target/actual sparsity trajectory, immediate loss, final accuracy, and best accuracy",
        "The notebook records a row at each schedule update rather than only the endpoint. Both routes clone the same baseline and execute the same number of optimizer steps. Mask refresh and reapplication are explicit, making it possible to distinguish a schedule failure from silent weight regrowth.",
        "Accept a schedule only when its complete trajectory, final support, quality recovery, and training budget are recorded and meet the target card.",
        "A schedule can appear better simply because it prunes later and spends more steps near the dense model. Comparing endpoints without integrating the actual sparsity trajectory hides that advantage. Small synthetic data also makes recovery unusually cheap.",
        "Match candidates by area under the sparsity-time curve, sweep update frequency and learning-rate restarts, and repeat on a downstream task metric rather than toy accuracy alone.",
        "Gradual pruning controls the sequence of support changes; its endpoint is insufficient to reproduce or judge recovery.",
        "pytorch-gpu",
        [("Dense accuracy", "dense_accuracy", "percent"), ("One-shot final accuracy", "oneshot_final_accuracy", "percent"), ("Gradual final accuracy", "gradual_final_accuracy", "percent"), ("Target sparsity", "target_sparsity", "percent"), ("Schedule updates", "schedule_updates", "int")],
        ["gradual_pruning", "tfmot"],
    ),
    L(
        12,
        "sparse-regularization-gates",
        "Sparse Regularization and Learnable Structural Gates",
        "Can training produce a reproducible structural ranking instead of choosing a threshold after the fact?",
        "Learnable gates attach a continuous variable to each channel and optimize it with the task. A sparsity penalty can separate useful and dispensable structures, but thresholding still creates a discrete architecture and must be evaluated. Gate values, penalty strength, temperature, and threshold belong in the artifact.",
        "A frozen feature tensor, a trainable gated linear predictor, per-channel sigmoid gates, a task loss, an L1 gate penalty, and a threshold sweep form the lab.",
        "With gate `g_c=sigmoid(a_c)`, a hidden feature becomes `g_c h_c`. Optimizing `L_task + lambda sum(g_c)` trades fit against active width. Sigmoid gates rarely become exact zeros, so deployment selects a threshold or top-k budget and physically rebuilds the layer. The continuous optimum and discrete candidate are different models; both losses must be measured. Gate scale can also trade with neighboring weights unless those degrees of freedom are controlled.",
        [
            "Regularization shapes a ranking but does not physically remove channels.",
            "Continuous and thresholded objectives must both be reported.",
            "Lambda and threshold define different parts of the sparsity trade-off.",
        ],
        [
            "Predict whether sigmoid gates reach exact zero under an L1 penalty.",
            "Predict how increasing lambda changes active channels and task loss.",
            "Choose a threshold using a frozen validation objective rather than the training batch.",
        ],
        "Train channel gates under two regularization strengths and evaluate a frozen threshold sweep.",
        "weak gate regularization with a mostly dense effective width",
        "stronger regularization plus discrete threshold candidates",
        "features, targets, predictor initialization, optimizer steps, thresholds, seed, and validation split",
        "gate distribution, active channels, continuous loss, thresholded loss, and selected threshold",
        "The feature generator deliberately makes only a subset of channels predictive. Two gate models begin from identical logits, and the threshold sweep evaluates held-out loss without further fitting. This isolates whether the learned ranking exposes the known sparse structure.",
        "Accept a gate-derived architecture only when threshold selection is frozen, held-out quality passes, and the physical model reproduces the gated candidate.",
        "Jointly trainable downstream weights can absorb inverse gate scaling, making raw gates misleading. Selecting lambda or threshold on the final test set leaks evaluation. Hard thresholding may also remove interacting channels that looked individually small.",
        "Add hard-concrete or top-k gates, rebuild a physical narrow layer, and test ranking stability across seeds and task slices.",
        "Learnable gates turn structure selection into optimization, but deployment still requires a separately validated discrete architecture.",
        "numerical-model",
        [("Weak active channels", "weak_active_channels", "int"), ("Strong active channels", "strong_active_channels", "int"), ("Weak gate mean", "weak_gate_mean", "float"), ("Strong gate mean", "strong_gate_mean", "float"), ("Selected threshold", "selected_threshold", "float"), ("Thresholded validation MSE", "thresholded_val_mse", "float")],
        ["network_slimming", "gradual_pruning"],
    ),
    L(
        13,
        "nm-2-4-sparsity",
        "N:M Semi-structured Sparsity and the 2:4 Contract",
        "Does a tensor with 50% zeros qualify for Sparse Tensor Core execution?",
        "NVIDIA's 2:4 path imposes a local pattern: within each group of four values along the required dimension, at least two are zero and the representation must be compressed for a supported sparse GEMM. Global sparsity, pattern compliance, backend conversion, and selected tactic are separate gates.",
        "A BF16 weight matrix, a random 50% mask, a magnitude-based exact 2:4 mask, a local compliance checker, ordinary dense timing, and an optional PyTorch semi-structured conversion probe are recorded.",
        "Partition the contracted dimension into groups of four and retain the two largest magnitudes per group. This guarantees exactly 2 nonzeros per group while preserving 50% globally. A random half mask only satisfies a fraction of groups. Even a compliant tensor remains dense storage until converted into the backend's compressed format, and the hardware/library combination must support its shape and dtype.",
        [
            "2:4 is checked per local group, not over the whole tensor.",
            "Pattern compliance precedes backend compression and tactic selection.",
            "Dense-path timing cannot establish sparse Tensor Core execution.",
        ],
        [
            "Predict the compliance of a random 50% mask.",
            "Prove that top-2-of-4 masking reaches exactly 50% sparsity.",
            "List the evidence needed after compliance before claiming acceleration.",
        ],
        "Compare random and exact 2:4 masks, then attempt a native semi-structured conversion without hiding incompatibility.",
        "random global 50% sparsity executed through ordinary dense matmul",
        "exact magnitude 2:4 sparsity plus an optional native conversion probe",
        "source weights, shape, dtype, input, zero budget, GPU, and timing protocol",
        "global sparsity, local compliance, dense-path latency, conversion availability, and conversion error",
        "The compliance function reshapes the K dimension into groups of four and counts nonzeros. The native conversion attempt is wrapped and stores either a successful sparse result or the exact exception text. Ordinary dense timing remains a control and is never relabeled as a sparse-kernel benchmark.",
        "Accept a 2:4 speed claim only after compliance, supported compression, a sparse operator trace, numerical validation, and a matched dense baseline all pass.",
        "Zeros can be arranged along the wrong axis, shapes can violate alignment, and a library can fall back to dense tactics. A failed PyTorch conversion on one stack does not imply the GPU lacks all 2:4 support; it bounds only that API path.",
        "Run the same compliant weights through cuSPARSELt or TensorRT, retain build logs and kernel names, and sweep supported shapes and FP16/BF16/INT8 dtypes.",
        "Exact 2:4 values are a necessary data invariant; native representation and tactic evidence complete the execution claim.",
        "compatibility-probe",
        [("Random-mask compliance", "random_compliance", "percent"), ("2:4 compliance", "nm_compliance", "percent"), ("2:4 sparsity", "nm_sparsity", "percent"), ("Dense baseline median", "dense_median_ms", "ms"), ("2:4 dense-path median", "nm_dense_path_median_ms", "ms"), ("Native conversion", "native_conversion_succeeded", "bool")],
        ["cusparselt", "tensorrt_sparse"],
    ),
    L(
        14,
        "pytorch-mask-lifecycle",
        "PyTorch Pruning API and the Complete Mask Lifecycle",
        "What changes in a module before and after `prune.remove`, and what must a rollback loader know?",
        "PyTorch pruning is a reparameterization lifecycle: apply a method, combine or update masks, train while preserving the mask, serialize the expected keys, optionally materialize with `remove`, and verify loading. Confusing a hook-based checkpoint with a materialized one breaks reproducibility.",
        "One CUDA linear layer is inspected at five points: dense, first mask, iterated mask, optimizer update, and removal. Named parameters, buffers, forward pre-hooks, sparsity, and output drift are captured.",
        "After pruning, `weight_orig` is a parameter and `weight_mask` a buffer; the visible `weight` is computed before forward. Iterative pruning combines masks through a pruning container. Gradients update `weight_orig`, so effective masked values remain zero at forward even when underlying values change. `remove` replaces this pair with a materialized `weight` parameter and deletes the hook; it does not undo pruning.",
        [
            "Parameter and buffer names encode the checkpoint lifecycle stage.",
            "Iterative masks compose rather than reset by default.",
            "Removal makes pruning permanent in a dense parameter.",
        ],
        [
            "Predict parameter and buffer names after the first mask.",
            "Predict sparsity after two iterative 25% pruning calls.",
            "Predict whether `remove` restores the deleted weights.",
        ],
        "Apply iterative PyTorch masks, take a training step, remove the reparameterization, and audit every state transition.",
        "dense module and single-mask state",
        "iteratively pruned, trained, and materialized module",
        "module, optimizer rule, input/target, pruning calls, seed, and inspection points",
        "effective sparsity, parameter names, buffer names, hook count, loss, and removal drift",
        "The notebook queries public module inspection APIs rather than inferring state from printed tensors. It records the effective weight before and after an optimizer step, then compares forward output immediately around `remove`. This produces a loader-oriented lifecycle trace.",
        "Accept a checkpoint only when its expected lifecycle stage, key schema, load procedure, mask policy, and rollback artifact are documented and tested.",
        "Loading a hook checkpoint into a plain module yields missing or unexpected keys. Optimizer state can point at reparameterized objects. Removing too early can allow zeros to regrow during later unconstrained training. These are state-management failures, not pruning-score failures.",
        "Save and reload both lifecycle variants in fresh modules, test optimizer resume, and add a schema version to the structured artifact.",
        "PyTorch masks are auditable training state; `remove` materializes them but does not create a sparse runtime format.",
        "pytorch-gpu",
        [("First-mask sparsity", "first_sparsity", "percent"), ("Iterated sparsity", "iterated_sparsity", "percent"), ("Parameters before remove", "parameters_before_remove", "str"), ("Buffers before remove", "buffers_before_remove", "str"), ("Hooks before remove", "hooks_before_remove", "int"), ("Remove max drift", "remove_max_error", "float")],
        ["pytorch_pruning", "determinism"],
    ),
    L(
        15,
        "depgraph-structured-pruning",
        "Torch-Pruning DepGraph: A Structured-Pruning Compatibility Lab",
        "Can a dependency graph identify every tensor coupled to one channel deletion on this environment?",
        "DepGraph turns a local root operation into a pruning group. That is precisely the bookkeeping manual structural pruning tends to miss. A credible lab must distinguish the graph concept, a manual CUDA control, and whether the optional Torch-Pruning package executed successfully on the recorded stack.",
        "A residual mini-network, one channel index set, a manually synchronized narrow copy, an import/version probe, and—when available—a real `DependencyGraph` group are the concrete objects.",
        "Torch-Pruning traces an example forward with autograd enabled, then maps a root pruning function through module and tensor dependencies. Group validation prevents deleting an entire dimension. The package mutates module structure, so saving a plain dense-definition state_dict is insufficient unless architecture metadata is reconstructed. The manual control proves expected shape propagation independently of package availability.",
        [
            "DepGraph groups coupled pruning operations from a root decision.",
            "Example inputs and enabled autograd define the traced dependency path.",
            "Package compatibility evidence is distinct from manual structural correctness.",
        ],
        [
            "Predict which modules join a group rooted at the first convolution.",
            "Predict the result when the optional package is absent.",
            "State what must be saved after module shapes are mutated.",
        ],
        "Build a real DepGraph group when available and always execute a manual CUDA structural-control path.",
        "manual synchronized pruning ledger for a residual mini-network",
        "Torch-Pruning dependency group and mutation when the package is available",
        "model, example input, root module, channel indices, eval mode, and GPU",
        "package availability/version, group validity/size, output shape, parameters, and caught exception",
        "The notebook first runs the manual control so the lesson remains informative on a minimal PyTorch installation. It then probes `torch_pruning`, builds the graph without `no_grad`, requests a pruning group, validates it, and records group detail instead of converting import failure into a successful backend claim.",
        "Accept the automated route only when the group is valid, forward and quality checks pass, and the mutated architecture has a tested save/load contract.",
        "A successful trace can miss data-dependent control flow or static attributes used outside tensor operations. A package import proves nothing about a specific model group. Conversely, package absence does not falsify the DepGraph method; it only leaves that native path unexecuted.",
        "Install the pinned Torch-Pruning version, run the notebook again, compare printed group operations with the manual ledger, and test whole-model serialization and reload.",
        "A dependency graph is valuable when its real group, mutation, and save/load path are observed—not when its name appears in a plan.",
        "compatibility-probe",
        [("Torch-Pruning available", "torch_pruning_available", "bool"), ("Group built", "group_built", "bool"), ("Group valid", "group_valid", "bool"), ("Manual output channels", "manual_output_channels", "int"), ("Manual parameters", "manual_parameters", "int"), ("Probe message", "probe_message", "str")],
        ["torch_pruning", "depgraph"],
    ),
    L(
        16,
        "keras-pruning-lifecycle",
        "TensorFlow MOT and the Keras Pruning/Export Lifecycle",
        "Why can training-time Keras sparsity fail to reduce a deployable TFLite artifact?",
        "TensorFlow Model Optimization wraps Keras layers with masks, thresholds, and pruning-step state. Export requires updating the pruning step during training, reaching the schedule, stripping wrappers, converting, and checking the final representation. This environment may not provide TensorFlow, so availability is an explicit result rather than an excuse for invented output.",
        "The lab records TensorFlow and TFMOT package availability, evaluates the polynomial schedule formula on CUDA, constructs the required lifecycle state machine, and runs a tiny native strip/export probe only when dependencies exist.",
        "Polynomial decay controls target sparsity by optimizer step. Wrappers contain training-only variables and callbacks update their step. `strip_pruning` removes wrappers while retaining sparse weights; it does not guarantee a smaller uncompressed format or a sparse-accelerated runtime. TFLite conversion and optional compression are separate gates. A native experiment must therefore preserve versions, wrapper state, stripped model, converted bytes, and output parity.",
        [
            "Schedule progress depends on optimizer steps and update callbacks.",
            "Stripping removes training wrappers, not necessarily dense storage.",
            "TensorFlow/TFMOT availability is part of reproducible backend evidence.",
        ],
        [
            "Predict the target sparsity before, during, and after a polynomial window.",
            "Predict what `strip_pruning` removes and what it retains.",
            "List the artifacts required before claiming a TFLite size benefit.",
        ],
        "Probe the Keras pruning stack and execute the schedule/lifecycle contract without fabricating a missing native backend.",
        "CUDA-evaluated polynomial schedule and an unstripped lifecycle state",
        "native TFMOT wrapper/strip probe when available, otherwise a bounded compatibility result",
        "environment, schedule endpoints, steps, target rate, lifecycle transitions, and seed",
        "package availability, schedule values, native probe status, and lifecycle gates",
        "The notebook uses the published polynomial form to produce deterministic target rates and checks that strip/export cannot be marked complete before training and wrapper removal. Conditional imports keep missing packages in a structured field. No Keras latency or TFLite size number is synthesized when the stack is absent.",
        "Accept a Keras pruning delivery only after native training, `UpdatePruningStep`, `strip_pruning`, TFLite conversion, output parity, and target-device measurement all pass.",
        "A numerical schedule is not a TensorFlow execution. Installing TensorFlow without a compatible GPU stack may move compute to CPU. A stripped model can retain dense tensors with zeros, and zip compression can be confused with runtime memory savings.",
        "Run the notebook in a pinned TensorFlow/TFMOT environment, retain wrapper and stripped summaries, convert to TFLite, and benchmark the exact target device.",
        "Keras pruning is a versioned train-strip-convert lifecycle; a missing native stack must remain visibly unexecuted.",
        "compatibility-probe",
        [("TensorFlow available", "tensorflow_available", "bool"), ("TFMOT available", "tfmot_available", "bool"), ("Mid-schedule sparsity", "mid_schedule_sparsity", "percent"), ("Final schedule sparsity", "final_schedule_sparsity", "percent"), ("Native probe executed", "native_probe_executed", "bool"), ("Lifecycle ready", "lifecycle_ready", "bool")],
        ["tfmot", "tf_strip"],
    ),
    L(
        17,
        "cpu-runtime-sparsity",
        "OpenVINO, NNCF, and Intel Runtime Sparsity",
        "Why can a generic sparse checkpoint miss the optimized CPU path?",
        "CPU deployment benefit depends on a pattern, graph transformation, precision, and operator implementation supported by the target OpenVINO/oneDNN stack. NNCF or Intel Neural Compressor configuration is part of the executable artifact; a PyTorch zero rate alone is not.",
        "The notebook probes OpenVINO, NNCF, and Neural Compressor packages, creates unstructured and filter-pruned controls on CUDA, and records a deployment gate matrix without asserting CPU speed from GPU evidence.",
        "Unstructured zeros preserve dense tensor dimensions unless a sparse encoding and sparse operator are selected. NNCF filter pruning can propagate structural changes and export a smaller graph, while post-training sparsity tools may target runtime-specific patterns. CPU SIMD utilization, threading, cache behavior, and quantization interact with width. Therefore the correct handoff includes model format, pattern, runtime version, thread settings, and operator log.",
        [
            "Framework zeros are not an OpenVINO execution plan.",
            "Filter removal and unstructured encoding expose different CPU opportunities.",
            "GPU control results cannot substitute for CPU runtime measurements.",
        ],
        [
            "Predict which package probes succeed in the recorded GPU environment.",
            "Explain why physically narrower shapes remain useful without a sparse CPU kernel.",
            "List the CPU-specific fields needed for a fair benchmark.",
        ],
        "Probe Intel compression/runtime packages and contrast value sparsity with physical width under a bounded evidence label.",
        "same-shape unstructured zero mask represented as a dense PyTorch tensor",
        "physically narrower dense control and optional OpenVINO/NNCF native path",
        "source tensor, zero budget, input, environment, package names, and decision gates",
        "package availability, logical sparsity, physical width, output drift, and native-run status",
        "The experiment keeps its CUDA numerical control separate from the package matrix. Conditional imports record exact availability; the conclusion remains `not_run` for OpenVINO performance unless a native model conversion and CPU workload execute. This prevents a generic pruning result from being laundered into an Intel deployment claim.",
        "Accept an Intel deployment only after conversion, graph inspection, CPU thread pinning, quality parity, and repeated target-CPU latency/throughput evidence.",
        "Package presence is weaker than operator support, and a laptop CPU result may not transfer to the production SKU. A narrow channel count can hurt vector alignment, while unstructured compression can reduce disk size without runtime benefit.",
        "Create a pinned OpenVINO/NNCF environment, export both candidates, inspect IR dimensions and operators, then benchmark several thread and batch settings on the actual CPU target.",
        "Intel sparsity is a runtime-specific graph and kernel decision; CUDA zeros provide only a numerical control.",
        "compatibility-probe",
        [("OpenVINO available", "openvino_available", "bool"), ("NNCF available", "nncf_available", "bool"), ("Neural Compressor available", "neural_compressor_available", "bool"), ("Logical sparsity", "logical_sparsity", "percent"), ("Physical width reduction", "width_reduction", "percent"), ("Native CPU run", "native_cpu_run", "bool")],
        ["openvino", "nncf"],
    ),
    L(
        18,
        "tensorrt-sparsity-gates",
        "TensorRT Sparse Deployment and Polygraphy Evidence",
        "What must the build log show before `--sparsity=enable` becomes an acceleration claim?",
        "TensorRT evaluates structured-sparsity eligibility and tactic profitability. A 2:4-compliant ONNX weight plus a sparsity flag makes a layer eligible; the builder can still select a dense tactic. Polygraphy can help inspect and transform models, but only engine logs and matched benchmarks establish execution.",
        "The lab creates a compliant convolution/linear-style weight, checks pattern and dtype gates, probes TensorRT and Polygraphy packages plus `trtexec`, and produces an eligibility-versus-selection matrix.",
        "TensorRT's structured sparsity requires the documented local weight pattern and supported FP16 or INT8 execution. The builder reports eligible layers separately from layers for which sparse tactics are chosen. `--sparsity=force` style mutation changes weights and therefore quality; enable mode should consume already compliant weights. Strong typing, shapes, workspace, and version influence tactic search.",
        [
            "Eligibility and tactic selection are separate log events.",
            "Forcing a pattern is a model mutation that needs quality validation.",
            "The engine version, flags, profiles, and timing cache identify the build.",
        ],
        [
            "Predict whether a compliant weight alone proves a sparse tactic ran.",
            "List the log lines and numerical checks required for acceptance.",
            "Explain why forced pruning must be treated as a new model candidate.",
        ],
        "Build the full pre-engine eligibility ledger and probe native TensorRT/Polygraphy tools on the RTX 5090 host.",
        "2:4-compliant BF16/FP16 weight and dense PyTorch numerical control",
        "native TensorRT build/tactic path when packages and `trtexec` are available",
        "weight, grouping axis, dtype gate, environment, package probes, and required build fields",
        "2:4 compliance, dtype eligibility, TensorRT/Polygraphy/trtexec availability, and native engine status",
        "The notebook can prove the data-side invariant on CUDA and can prove whether native tools exist. It cannot infer a TensorRT engine from PyTorch timing. The gate dictionary leaves engine build and sparse-tactic selection false unless those events actually occur.",
        "Accept TensorRT sparsity only with a valid engine, explicit eligible and selected sparse-tactic logs, output parity, and matched dense/sparse engine benchmarks.",
        "A builder flag can be ignored by ineligible layers or lose tactic search to a faster dense kernel. Dynamic profiles may select different tactics, and a successful build on A100 does not establish behavior on RTX 5090.",
        "Use a TensorRT-enabled container, export the compliant model, retain Polygraphy inspection plus verbose build logs, and benchmark every production optimization profile.",
        "TensorRT sparsity is proven by eligibility, selected tactic, correctness, and benchmark evidence—not by a flag.",
        "compatibility-probe",
        [("2:4 compliance", "nm_compliance", "percent"), ("Dtype eligible", "dtype_eligible", "bool"), ("TensorRT available", "tensorrt_available", "bool"), ("Polygraphy available", "polygraphy_available", "bool"), ("trtexec available", "trtexec_available", "bool"), ("Sparse engine built", "sparse_engine_built", "bool")],
        ["tensorrt_sparse", "cusparselt"],
    ),
    L(
        19,
        "onnx-shape-consistency",
        "ONNX Export, Graph Repair, and Shape Consistency",
        "Can a pruned PyTorch model run correctly while its exported graph carries inconsistent channel metadata?",
        "Physical pruning changes dimensions across weights, bias, normalization, reshape, concat, and post-processing nodes. ONNX export success only serializes the traced path; checker, shape inference, ONNX Runtime parity, and explicit dimension audits establish a deployable graph.",
        "A small physically pruned multi-input model is exported in memory/on disk, checked with ONNX, passed through shape inference, executed with ONNX Runtime when available, and compared with CUDA/PyTorch output.",
        "Static tensor shapes encode known dimensions while dynamic axes use symbolic parameters. Shape inference propagates what operator schemas can prove but cannot resolve every data-dependent reshape. `onnx.checker.check_model(..., full_check=True)` validates graph structure and types; ONNX Runtime supplies an independent execution path. After channel deletion, every initializer and consumer dimension must agree with the new graph contract.",
        [
            "Export, checker, inference, and runtime parity are distinct gates.",
            "Physical channel changes must reach initializers and consumer shapes.",
            "Dynamic symbols do not excuse inconsistent known dimensions.",
        ],
        [
            "Predict the output channel dimension after the physical slice.",
            "Explain what ONNX shape inference can and cannot establish.",
            "Choose a parity tolerance for the independent runtime output.",
        ],
        "Export a physically pruned CUDA model, run ONNX checker and shape inference, and compare ONNX Runtime output.",
        "PyTorch output and declared pruned shape ledger",
        "checked/inferred ONNX graph plus ONNX Runtime execution",
        "weights, retained indices, inputs, opset, dynamic-axis policy, dtype, and tolerance",
        "export status, checker status, inferred shapes, initializer dimensions, runtime status, and max error",
        "The notebook writes the ONNX model into the lesson artifact directory, invokes full checking, records inferred value shapes, and runs the same inputs through ONNX Runtime. Exceptions are caught into structured fields, but success requires every gate and numerical parity rather than export alone.",
        "Accept the exported graph only when checker, shape inference audit, initializer dimensions, and target-runtime parity all pass.",
        "Tracer warnings and constant folding can hide data-dependent behavior. Shape inference may remain partial, and ONNX Runtime success does not guarantee TensorRT support. Multi-profile production shapes must be tested separately.",
        "Add dynamic batch and sequence axes, deliberately corrupt one initializer to verify the audit fails, then test the repaired graph in the final deployment runtime.",
        "A pruned ONNX graph is deliverable only after structural checks and independent runtime parity confirm the new shape contract.",
        "native-backend",
        [("ONNX available", "onnx_available", "bool"), ("Export succeeded", "export_succeeded", "bool"), ("Checker passed", "checker_passed", "bool"), ("Shape inference passed", "shape_inference_passed", "bool"), ("ORT executed", "ort_executed", "bool"), ("ORT max error", "ort_max_error", "float"), ("ONNX bytes", "onnx_bytes", "bytes")],
        ["onnx_checker", "onnx_shapes"],
    ),
    L(
        20,
        "resnet-channel-pruning",
        "CNN Case Study: ResNet Channel Pruning",
        "Why can a ResNet-like block lose parameters without reaching the expected throughput?",
        "Residual networks tie channel width to additions and projection shortcuts. A safe case study must rebuild a whole stage-compatible block, preserve the add contract, update the classifier or downstream consumer, and benchmark several batches. The percentage of removed channels is only the starting point.",
        "A compact ResNet-style stem, residual block with projection, global pooling, and classifier is built in full and narrow variants. Weights are copied by retained indices where functions align, and structural FLOPs, parameters, output error, and latency are measured.",
        "Within a basic residual block, both the main path's final convolution and shortcut projection produce the same channel count. Narrowing the stage changes subsequent convolutions and classifier input. FLOPs fall roughly with channel products, but latency also depends on convolution algorithm, memory layout, launch overhead, and batch. A batch-1 result and a batch-64 throughput result answer different deployment questions.",
        [
            "Residual additions impose equal output widths.",
            "Stage pruning propagates into later layers and the classifier.",
            "FLOP reduction and throughput gain need separate measurements.",
        ],
        [
            "Predict every module dimension changed by halving the stage width.",
            "Predict whether the percentage FLOP and latency reductions match exactly.",
            "Choose batch-specific gates for interactive and throughput services.",
        ],
        "Construct full and half-width ResNet-like models and compare structure, parity control, batch-1 latency, and batch-64 throughput.",
        "full-width ResNet-style stage",
        "physically half-width stage with synchronized main, shortcut, and classifier dimensions",
        "input resolution, stem, depth, retained indices, dtype, GPU, warm-up, repetitions, and batches",
        "parameters, analytical convolution/linear FLOPs, batch-1 latency, batch-64 throughput, and output drift",
        "The model is intentionally small enough for a repeatable notebook while preserving the dependency pattern that makes ResNet pruning nonlocal. Structural counters read actual module shapes. Timing uses the same CUDA-event helper at both batches; the notebook does not project ImageNet Top-1 or ResNet-50 speed from this mini-network.",
        "Accept a ResNet pruning candidate only when stage dependencies, task quality, target batches, and end-to-end runtime all pass against the exact baseline revision.",
        "Toy random weights make output drift a bookkeeping signal rather than a quality score. Widths can cross library-alignment thresholds, and data loading or post-processing can dominate a production service. A small block cannot prove ResNet-50 throughput.",
        "Apply the same ledger to a pretrained torchvision ResNet, calibrate importance on real data, fine-tune, and profile operator shapes at production batches.",
        "ResNet channel pruning is a stage-level graph transformation whose benefit must be measured at each target workload.",
        "pytorch-gpu",
        [("Full parameters", "full_parameters", "int"), ("Narrow parameters", "narrow_parameters", "int"), ("FLOP reduction", "flop_reduction", "percent"), ("Batch-1 full median", "batch1_full_median_ms", "ms"), ("Batch-1 narrow median", "batch1_narrow_median_ms", "ms"), ("Batch-64 speedup", "batch64_speedup", "ratio")],
        ["resnet", "depgraph"],
    ),
    L(
        21,
        "detection-segmentation-safety",
        "Safe Pruning for Detection and Segmentation",
        "Can an unchanged average metric hide a large regression on small objects or a rare mask class?",
        "Detection and segmentation heads consume multi-scale features, and business risk is rarely uniform across sizes and classes. A pruning candidate can preserve an aggregate proxy while degrading the feature-pyramid level responsible for small objects. Safety therefore requires slice metrics and per-branch budgets.",
        "A three-scale feature-pyramid toy head, synthetic large/medium/small targets, a uniform-pruning candidate, a protected-high-resolution candidate, and per-slice reconstruction errors form the controlled lab.",
        "High-resolution pyramid features carry more spatial positions and often serve small objects. If aggregate loss weights every tensor element or sample uniformly, a large branch can dominate or a rare slice can disappear in the mean. Define `E_slice` separately and an acceptance rule such as `max slice regression <= tau` in addition to aggregate change. Budget allocation then becomes risk-weighted rather than purely parameter-weighted.",
        [
            "Multi-scale branches have different semantic responsibilities.",
            "Aggregate quality can pass while a protected slice fails.",
            "Risk-weighted budgets require explicit slice thresholds.",
        ],
        [
            "Predict which pyramid branch is most sensitive for the small-object proxy.",
            "Construct an example where mean error falls but worst-slice error rises.",
            "Choose both aggregate and slice-level rollback gates.",
        ],
        "Compare uniform channel pruning with a high-resolution-protected budget at equal total retained channels.",
        "uniform pruning across three feature-pyramid branches",
        "risk-weighted pruning that protects the high-resolution/small-object branch",
        "feature tensors, targets, total retained-channel budget, head weights, seed, and slice definitions",
        "aggregate error, large/medium/small slice error, worst-slice regression, and retained channels",
        "The notebook constructs target outputs so each slice depends most strongly on its corresponding scale. Both candidates spend the same total channel budget, but allocate it differently. Reporting every slice next to the aggregate exposes whether the protected policy trades average error for a safer worst case.",
        "Accept only when aggregate detection/segmentation quality and every business-critical size/class slice stay within frozen thresholds.",
        "A synthetic reconstruction proxy is not COCO AP, mask AP, recall, or calibration. Slice definitions chosen after observing failures can overfit the report. Feature channels also interact across the neck and head in real architectures.",
        "Run the policy on a real detector with COCO `AP`, `AP_S`, `AP_M`, `AP_L`, class recall, and mask metrics, then bind each gate to a rollback action.",
        "Safe pruning treats the worst critical slice as a first-class constraint rather than trusting an aggregate metric.",
        "numerical-model",
        [("Uniform aggregate RMSE", "uniform_aggregate_rmse", "float"), ("Protected aggregate RMSE", "protected_aggregate_rmse", "float"), ("Uniform small-slice RMSE", "uniform_small_rmse", "float"), ("Protected small-slice RMSE", "protected_small_rmse", "float"), ("Worst uniform slice", "uniform_worst_slice", "str"), ("Total retained channels", "retained_channels", "int")],
        ["coco", "depgraph"],
    ),
    L(
        22,
        "transformer-structure-pruning",
        "Pruning Transformer Heads, FFN Neurons, and Layers",
        "Which structural unit changes Transformer compute rather than only masking values?",
        "Attention heads, FFN intermediate neurons, hidden dimensions, and whole layers are different dependency units. Masking a head preserves the packed QKV and output projection shapes, while physically reducing FFN width changes dense GEMMs. Whole-layer removal changes depth and residual composition. Each route needs its own quality and latency evidence.",
        "A compact pre-norm Transformer block exposes head outputs, an FFN intermediate, residuals, and a two-block stack. The lab compares a head mask, a half-width physical FFN, and layer skipping under one CUDA workload.",
        "For sequence length S and hidden width D, attention projections scale roughly with `S D²` and score/value work with `S² D`; FFN work scales with `S D D_ff`. Removing a logical head but retaining packed D-wide projections may leave most work unchanged. Halving D_ff directly reduces two GEMM dimensions. Removing a block deletes both attention and FFN work but creates a larger functional perturbation. Structural claims must specify which dimensions changed.",
        [
            "A head mask is not automatically a narrower attention operator.",
            "FFN width directly controls two dense matrix multiplications.",
            "Layer pruning changes depth and residual transformations.",
        ],
        [
            "Predict which candidate changes physical parameter count.",
            "Estimate the relative attention and FFN work for the chosen S, D, and D_ff.",
            "Predict which route has the largest output drift before recovery.",
        ],
        "Measure head masking, physical FFN narrowing, and whole-layer skipping in a compact CUDA Transformer.",
        "full block/stack and same-shape attention-head masking",
        "physically narrowed FFN and one-layer-shorter stack",
        "weights where comparable, input, sequence length, batch, hidden width, dtype, eval mode, and timing",
        "physical parameters, output RMSE/cosine, median latency, and theoretical work components",
        "The block returns a head-mask path without rewriting packed projections, making its unchanged physical shape visible. The FFN candidate copies selected intermediate rows/columns into smaller linear modules. Layer skipping reuses the first block output. These controls keep three pruning units conceptually separate.",
        "Accept a Transformer structure only after task/perplexity gates and a runtime trace confirm that the intended dimensions or depth changed.",
        "Random weights do not reveal head redundancy. Fused attention kernels may require fixed head dimensions or grouped-query layouts, and KV-cache shape couples attention structure to serving memory. Layer removal can shift normalization statistics and generation behavior.",
        "Repeat on a pretrained encoder or decoder, evaluate task quality/perplexity and KV-cache bytes, and use a backend with explicit variable-head or narrowed-FFN support.",
        "Transformer pruning must name the structural unit and prove its physical compute path; masks alone are insufficient.",
        "pytorch-gpu",
        [("Full parameters", "full_parameters", "int"), ("FFN-narrow parameters", "ffn_narrow_parameters", "int"), ("Head-mask RMSE", "head_mask_rmse", "float"), ("FFN-narrow RMSE", "ffn_narrow_rmse", "float"), ("Layer-skip RMSE", "layer_skip_rmse", "float"), ("Full median", "full_median_ms", "ms"), ("FFN-narrow median", "ffn_narrow_median_ms", "ms")],
        ["head_pruning", "depgraph"],
    ),
    L(
        23,
        "sparsegpt-wanda",
        "One-shot LLM Pruning: SparseGPT and Wanda Mechanisms",
        "Why should calibration activations change which LLM weights survive?",
        "One-shot LLM pruning must choose a support without full retraining. Magnitude ignores input usage; Wanda combines weight magnitude with activation norms; SparseGPT uses second-order reconstruction information and sequential compensation. A small layer can expose these objectives without pretending to reproduce a 70B run.",
        "A wide linear projection, calibration tokens with deliberately uneven feature scales, held-out tokens, magnitude scores, Wanda scores, a diagonal-curvature proxy, and equal-sparsity reconstructed outputs form the lab.",
        "For `Y=XW^T`, Wanda scores weight `w_ij` by `|w_ij| ||X_:j||`, so a modest weight on a frequently excited feature may outrank a larger unused weight. SparseGPT instead minimizes layer reconstruction with an approximate Hessian and updates remaining weights as columns are pruned. A diagonal `X^T X` proxy can illustrate sensitivity but omits the inverse-Hessian sequential algorithm. Equal sparsity and held-out output error are required for comparison.",
        [
            "Calibration activations define feature importance for one-shot pruning.",
            "Wanda scoring and SparseGPT compensation are not the same algorithm.",
            "Toy layer reconstruction cannot establish 70B perplexity or speed.",
        ],
        [
            "Predict how rescaling one input feature changes Wanda but not magnitude ranking.",
            "Explain what the diagonal-curvature proxy omits from SparseGPT.",
            "Choose calibration and held-out splits for a fair one-shot comparison.",
        ],
        "Compare magnitude, Wanda, and diagonal-curvature masks at identical 50% sparsity on held-out layer outputs.",
        "plain magnitude one-shot pruning",
        "Wanda activation-aware scoring and a diagonal-curvature sensitivity proxy",
        "weights, calibration tokens, held-out tokens, sparsity, grouping policy, and seed",
        "held-out RMSE, cosine similarity, support overlap, sparsity, and calibration feature scales",
        "The notebook constructs calibration features with unequal energy so activation-aware methods have a measurable signal. Each scoring rule retains the same number of weights per row. Output metrics are computed on separate held-out tokens. The artifact explicitly labels the curvature route a proxy rather than SparseGPT.",
        "Accept an LLM pruning method only after frozen calibration, full-model perplexity/zero-shot gates, serialization, and a supported sparse inference path are measured.",
        "Calibration domains can bias activation norms, and per-row toy masks omit blockwise sequential compensation. Lower layer RMSE may not preserve generation, rare capabilities, or safety. Unstructured zeros may still run dense.",
        "Run official SparseGPT and Wanda implementations on a pinned open model, sweep calibration domains and sparsity patterns, then benchmark a named sparse runtime separately from quality.",
        "Activation-aware support selection can reduce one-shot reconstruction error, but official algorithms and full-model evidence remain separate gates.",
        "numerical-model",
        [("Magnitude RMSE", "magnitude_rmse", "float"), ("Wanda RMSE", "wanda_rmse", "float"), ("Curvature-proxy RMSE", "curvature_rmse", "float"), ("Magnitude cosine", "magnitude_cosine", "float"), ("Wanda cosine", "wanda_cosine", "float"), ("Support overlap", "magnitude_wanda_overlap", "percent")],
        ["sparsegpt", "wanda"],
    ),
    L(
        24,
        "compression-order",
        "Ordering Distillation, Quantization, and Pruning",
        "Does pruning before calibration produce the same quantized model as pruning after calibration?",
        "Compression operators do not generally commute. Pruning changes distributions and structure; quantization freezes scales or codebooks; distillation changes the recovery objective. A combination roadmap should compare explicit orders at the same storage/compute budget rather than concatenating technique names.",
        "A linear teacher, student weight, calibration inputs, held-out inputs, magnitude pruning, symmetric INT8 fake quantization, two operator orders, and an optional short distillation recovery are compared.",
        "Let P be pruning and Q_s quantization under scale s. If s is calibrated on dense W, then `P(Q_s(W))` uses a range influenced by values later deleted. `Q_{s'}(P(W))` recalibrates after pruning and can use a different step. They are equal only under special masks and scales. Distillation adds a loss on teacher outputs and should occur while the student's actual compression constraints are active if it is meant to recover that candidate.",
        [
            "Pruning and calibrated quantization are generally non-commutative.",
            "Every order needs its own calibration and recovery protocol.",
            "Combination fairness requires equal final budgets and held-out metrics.",
        ],
        [
            "Predict whether dense-calibrated and prune-calibrated INT8 scales match.",
            "Write the two operator compositions and identify their different state.",
            "Choose when a teacher loss should observe the compressed student.",
        ],
        "Compare prune-then-quantize, quantize-then-prune, and constrained distillation recovery at one final sparsity/bit budget.",
        "quantize dense weights with a dense-calibrated scale, then prune",
        "prune first, recalibrate INT8, and optionally recover under teacher outputs",
        "teacher, starting student, calibration/held-out tensors, sparsity, quantizer, recovery steps, and seed",
        "quantization scales, held-out RMSE/cosine, final sparsity, and recovery improvement",
        "The notebook stores both scales and masks so the order is auditable. The short recovery optimizes the materialized pruned/quantized simulation against teacher outputs and reapplies the mask. This illustrates a combination dependency, not full QAT or production distillation.",
        "Accept a compression order only after its calibration data, recovery objective, final representation, quality, and runtime are tested as one immutable recipe.",
        "Allowing one route to recalibrate or recover while the other cannot makes the order comparison unfair. Fake quantization does not establish an INT8 kernel. A tiny teacher-student layer cannot predict end-to-end task accuracy.",
        "Create a factorial experiment over order, calibration source, and recovery; then export every final candidate to the same backend and compare storage, quality, latency, and operational complexity.",
        "Compression order is part of the model recipe because pruning, calibration, and recovery state do not commute.",
        "numerical-model",
        [("Quantize-first scale", "quantize_first_scale", "float"), ("Prune-first scale", "prune_first_scale", "float"), ("Quantize-first RMSE", "quantize_first_rmse", "float"), ("Prune-first RMSE", "prune_first_rmse", "float"), ("Recovered RMSE", "recovered_rmse", "float"), ("Final sparsity", "final_sparsity", "percent")],
        ["distillation", "deep_compression"],
    ),
    L(
        25,
        "sparsity-benchmarking",
        "Benchmarking Sparsity: Proving a Real Speedup",
        "Which benchmark prevents a lower mean from hiding an unchanged p99 or memory peak?",
        "Sparse acceleration is a runtime claim. The protocol must freeze shapes, batches, dtype, warm-up, synchronization, sample window, power state, and backend. It should report a distribution and throughput, plus memory and operator evidence, rather than a single average.",
        "Dense, same-shape masked, and physically narrowed linear blocks are timed with retained per-iteration CUDA-event samples at batch 1 and batch 64. Median, p95, p99, throughput, and peak memory are computed.",
        "GPU work is asynchronous, so host timing without synchronization measures enqueue cost. Warm-up absorbs initialization and algorithm selection. Percentiles require sorted repeated samples; `p99` is unstable with too few points. Throughput is workload completed per unit time and should be measured at the serving batch, not derived from peak FLOPs. Peak memory must be reset around each candidate.",
        [
            "Benchmark identity includes workload, backend, and timing semantics.",
            "Mean, median, and tail latency can rank candidates differently.",
            "Masked dense and physically narrow controls distinguish zeros from less work.",
        ],
        [
            "Predict which candidate changes dense operator dimensions.",
            "Explain why 20 samples are weak evidence for p99.",
            "Choose a warm-up and sampling protocol before reading results.",
        ],
        "Benchmark dense, masked, and narrowed candidates across latency and throughput batches with retained samples.",
        "dense full-width and same-shape 75%-masked dense execution",
        "physically quarter-width dense execution",
        "GPU, clocks as observed, shapes, weights, dtype, batches, warm-up, samples, and synchronization",
        "p50/p95/p99 latency, throughput, peak memory, shape, and speedup",
        "The timing helper allocates tensors before measurement, warms each candidate, and records individual CUDA-event durations. Summary functions preserve raw samples in the JSON artifact. A separate large batch prevents single-request latency from masquerading as service throughput.",
        "Accept a speedup only when the matched baseline, tail gate, throughput gate, memory gate, and operator/shape evidence all meet the frozen protocol.",
        "Shared GPU load, dynamic clocks, allocator history, and insufficient samples can move tails. Microbenchmarks omit data movement and service queues. A narrower toy layer cannot prove an end-to-end model gain.",
        "Repeat in an isolated process, capture Nsight or profiler operator names, add confidence intervals, and run a representative end-to-end service load with request arrivals.",
        "A sparsity speedup is a distribution measured on the intended execution path, not a zero count or a best-case sample.",
        "pytorch-gpu",
        [("Dense p50", "dense_p50_ms", "ms"), ("Dense p99", "dense_p99_ms", "ms"), ("Masked p50", "masked_p50_ms", "ms"), ("Narrow p50", "narrow_p50_ms", "ms"), ("Narrow p99", "narrow_p99_ms", "ms"), ("Batch-64 speedup", "batch64_speedup", "ratio"), ("Samples", "samples", "int")],
        ["pytorch_benchmark", "pytorch_profiler"],
    ),
    L(
        26,
        "accuracy-recovery-rollback",
        "Accuracy Recovery, Rollback, and Slice Error Analysis",
        "What should happen when overall accuracy recovers but one long-tail class remains below its rollback threshold?",
        "Recovery is not one scalar. A pruned model may recover aggregate accuracy by favoring common classes while a rare or high-risk class remains degraded. The release artifact must preserve the dense baseline, pruned checkpoint identity, confusion matrix, slice deltas, acceptance thresholds, and deterministic rollback decision.",
        "An imbalanced three-class CUDA classification task, dense checkpoint, magnitude-pruned candidate, short recovery, confusion matrices, per-class recall, aggregate accuracy, and a frozen worst-class gate form the experiment.",
        "Accuracy weights each example equally, so a 5% class can fall sharply while changing the aggregate by less than one point. Per-class recall `TP_c/(TP_c+FN_c)` and a confusion matrix expose the shift. The gate can require both `accuracy_drop <= a` and `min recall_drop >= -r`. Recovery selects the best checkpoint only on validation criteria; the rollback revision remains immutable.",
        [
            "Aggregate recovery can conceal minority-class regression.",
            "Thresholds must be frozen before the candidate is evaluated.",
            "Rollback is a derived decision linked to an immutable baseline.",
        ],
        [
            "Predict which class contributes least to aggregate accuracy.",
            "Compute recall from one confusion-matrix row.",
            "Write a two-part acceptance rule that protects the tail.",
        ],
        "Prune and recover an imbalanced classifier, then derive release or rollback from aggregate and per-class gates.",
        "dense checkpoint with frozen validation confusion matrix",
        "70%-pruned recovered checkpoint evaluated under the same split",
        "dataset, imbalance, checkpoint, mask, recovery steps, seed, thresholds, and evaluation code",
        "aggregate accuracy, per-class recall, worst recall drop, confusion matrices, and rollback decision",
        "The notebook trains a compact baseline, freezes it, prunes a clone, and records both immediate and recovered metrics. Confusion matrices are computed explicitly on GPU predictions and stored as lists. The final decision is programmatically derived from the predeclared aggregate and tail thresholds.",
        "Release only when every frozen aggregate and critical-slice gate passes; otherwise select the recorded dense revision as rollback.",
        "A toy class imbalance is not a production taxonomy, and recall alone ignores precision or calibration. Choosing thresholds after seeing the candidate invalidates the gate. Recovery on the final test split leaks evaluation.",
        "Add precision, calibration, and domain slices; separate train/validation/test; rehearse the actual model-registry rollback and monitor the same gates during canary.",
        "Recovery is complete only when aggregate and protected slices pass a predeclared gate with a tested rollback path.",
        "pytorch-gpu",
        [("Dense accuracy", "dense_accuracy", "percent"), ("Pruned immediate accuracy", "pruned_immediate_accuracy", "percent"), ("Recovered accuracy", "recovered_accuracy", "percent"), ("Worst recall drop", "worst_recall_drop", "percent"), ("Rollback required", "rollback_required", "bool"), ("Final sparsity", "final_sparsity", "percent")],
        ["determinism", "deep_compression"],
    ),
    L(
        27,
        "reproducible-experiments",
        "Automated Experiment Management and Reproducible Pruning Records",
        "Which fields make a pruning mask reproducible by another person?",
        "A sparsity number cannot identify a run. Reproduction needs data and model revisions, seed, score rule, tie behavior, target, mask bytes or hash, optimizer/recovery schedule, software, hardware, export command, and measured artifacts. Tracking systems help only when these fields are logged.",
        "A deterministic pruning function is executed twice with one seed and once with another. Configuration JSON, weight initialization, mask, output, and SHA-256 digests are compared in a small run registry.",
        "Random seed controls initialization and sampled data, but deterministic algorithms and stable ordering also matter. Hashing canonical JSON catches configuration drift; hashing contiguous mask bytes identifies the exact support. A code commit and environment complete the provenance. Reproducing the same global sparsity with a different mask is not the same experiment.",
        [
            "Sparsity equality is weaker than mask identity.",
            "Canonical configuration and binary artifacts need separate hashes.",
            "A tracking UI cannot compensate for missing provenance fields.",
        ],
        [
            "Predict which hashes match across identical-seed runs.",
            "Predict whether a different seed can preserve sparsity while changing the mask.",
            "List the minimum fields another machine needs to repeat the result.",
        ],
        "Run a pruning pipeline twice identically and once with a changed seed, then compare config, mask, and output hashes.",
        "two executions with identical canonical configuration and seed",
        "one execution changing only the seed",
        "algorithm code, config schema, dimensions, target sparsity, dtype, hash method, and environment capture",
        "config hash, mask hash, output hash, sparsity, same-seed equality, and changed-seed difference",
        "The notebook serializes configuration with sorted keys and compact separators before hashing. Masks move to CPU as contiguous bytes for a stable digest. The registry rows include environment and conclusion fields suitable for MLflow or W&B, but no external service is required to reproduce the core evidence.",
        "Accept a reproduction claim only when an independent rerun matches the declared configuration, mask or bounded metrics, and environment-sensitive tolerances.",
        "Seeds do not guarantee bitwise equality across all devices, library versions, or nondeterministic kernels. Hashing only a filename or sparsity misses content changes.",
        "Log the same schema to MLflow or W&B, rerun on a second machine, define which fields must match exactly versus within tolerance, and add checkpoint/export hashes.",
        "Reproducible pruning identifies the exact configuration, support, environment, and outputs—not merely the final zero percentage.",
        "pytorch-gpu",
        [("Same-seed config match", "same_seed_config_match", "bool"), ("Same-seed mask match", "same_seed_mask_match", "bool"), ("Same-seed output match", "same_seed_output_match", "bool"), ("Different-seed mask differs", "different_seed_mask_differs", "bool"), ("Sparsity", "sparsity", "percent"), ("Mask SHA-256", "mask_sha256", "short_hash")],
        ["determinism", "mlflow"],
    ),
    L(
        28,
        "edge-vs-server",
        "Why Edge and Server Deployment Need Different Pruning Strategies",
        "Should one sparse checkpoint be expected to win on both a phone and a GPU service?",
        "Edge devices often prioritize package bytes, cold start, peak memory, energy, and standard mobile operators. GPU services prioritize batch throughput, tail latency, concurrency, and kernel support. The same zeros can compress well for one platform and execute as an unchanged dense operator on another.",
        "The final lab combines measured RTX 5090 batch-1/batch-64 timing for dense, masked, and physically narrowed candidates with a transparent storage ledger and a platform decision matrix. Edge runtime numbers remain unmeasured.",
        "A masked dense matrix can reduce compressed bytes because zeros have low entropy while retaining M, N, and K on the GPU. A physically narrow model reduces dense arithmetic and activation width but changes architecture and may need more recovery. On edge, supported TFLite/OpenVINO operators and cold-start memory may dominate; on server, batching can amortize launch overhead and expose GEMM efficiency. Each platform therefore has distinct gates and can select a different candidate.",
        [
            "Platform objectives weight storage, latency, throughput, and energy differently.",
            "Compressed bytes do not predict GPU dense-path speed.",
            "Unmeasured edge metrics must remain `not run` in the decision matrix.",
        ],
        [
            "Predict which candidate has the smallest compressed weight payload.",
            "Predict which candidate changes GPU dense GEMM dimensions.",
            "Write separate acceptance gates for an edge app and a batched GPU service.",
        ],
        "Measure GPU candidates at interactive and throughput batches, calculate storage representations, and derive platform-specific decisions without inventing edge benchmarks.",
        "full-width dense and same-shape 75%-masked weight",
        "physically quarter-width dense candidate plus separate edge/server decision rows",
        "source weights, input widths, dtype, compression method, GPU timing, batches, and platform gate definitions",
        "raw/gzip bytes, batch-1 latency, batch-64 throughput, physical dimensions, edge evidence status, and platform decisions",
        "The notebook serializes identical candidate weights into raw in-memory payloads and gzip-compresses them, then measures the CUDA operators. It populates the edge row with storage facts but leaves device latency and energy unexecuted. The server row uses only measured RTX 5090 evidence. This prevents cross-platform projection.",
        "Choose a platform candidate only when every metric required by that platform has native evidence; otherwise leave the decision pending and preserve the dense rollback.",
        "Gzip is not a TFLite sparse encoding, RTX timing is not phone timing, and one server batch does not represent concurrency. A physically narrow shape can also be unsupported by a fixed mobile graph or misaligned on a GPU kernel.",
        "Export all candidates to TFLite/OpenVINO and a server backend, benchmark the actual phone/CPU/GPU targets including energy and concurrency, then compare total cost rather than transferring proxy results.",
        "Sparsity strategy is platform-specific: storage evidence, edge execution, and server execution must remain separate until each is measured.",
        "capacity-model",
        [("Dense gzip bytes", "dense_gzip_bytes", "bytes"), ("Masked gzip bytes", "masked_gzip_bytes", "bytes"), ("Narrow gzip bytes", "narrow_gzip_bytes", "bytes"), ("GPU batch-1 dense", "batch1_dense_ms", "ms"), ("GPU batch-1 narrow", "batch1_narrow_ms", "ms"), ("GPU batch-64 speedup", "batch64_speedup", "ratio"), ("Edge runtime measured", "edge_runtime_measured", "bool")],
        ["tflite", "tensorrt_sparse"],
    ),
]
