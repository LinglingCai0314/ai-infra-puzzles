#!/usr/bin/env python3
"""Original reader-facing curriculum for Chapter 03: vLLM inference systems."""

from __future__ import annotations

from typing import Any


COMMON_REFS = {
    "overview": ("vLLM documentation", "https://docs.vllm.ai/en/latest/"),
    "install": ("vLLM GPU installation", "https://docs.vllm.ai/en/latest/getting_started/installation/gpu/"),
    "quickstart": ("vLLM quickstart", "https://docs.vllm.ai/en/latest/getting_started/quickstart/"),
    "paged": ("PagedAttention paper", "https://arxiv.org/abs/2309.06180"),
    "engine_args": ("vLLM engine arguments", "https://docs.vllm.ai/en/latest/configuration/engine_args/"),
    "sampling": ("vLLM SamplingParams API", "https://docs.vllm.ai/en/latest/api/vllm/sampling_params/"),
    "openai": ("OpenAI-compatible server", "https://docs.vllm.ai/en/latest/serving/openai_compatible_server/"),
    "parallel": ("vLLM parallelism and scaling", "https://docs.vllm.ai/en/latest/serving/parallelism_scaling/"),
    "apc": ("Automatic prefix caching", "https://docs.vllm.ai/en/latest/features/automatic_prefix_caching/"),
    "quant": ("vLLM quantization", "https://docs.vllm.ai/en/latest/features/quantization/"),
    "kv_quant": ("Quantized KV cache", "https://docs.vllm.ai/en/latest/features/quantization/quantized_kvcache/"),
    "lora": ("LoRA adapters", "https://docs.vllm.ai/en/latest/features/lora/"),
    "spec": ("Speculative decoding", "https://docs.vllm.ai/en/latest/features/speculative_decoding/"),
    "structured": ("Structured outputs", "https://docs.vllm.ai/en/latest/features/structured_outputs/"),
    "models": ("Supported models", "https://docs.vllm.ai/en/latest/models/supported_models/"),
    "bench": ("vLLM benchmarking CLI", "https://docs.vllm.ai/en/latest/cli/bench/"),
    "metrics": ("Production metrics", "https://docs.vllm.ai/en/latest/usage/metrics/"),
    "docker": ("Deploying vLLM with Docker", "https://docs.vllm.ai/en/latest/deployment/docker/"),
    "k8s": ("vLLM production stack", "https://docs.vllm.ai/en/latest/deployment/integrations/production-stack/"),
    "disagg": ("Disaggregated prefill", "https://docs.vllm.ai/en/latest/features/disagg_prefill/"),
    "security": ("vLLM security policy", "https://github.com/vllm-project/vllm/security/policy"),
    "hf_config": ("Transformers model configuration", "https://huggingface.co/docs/transformers/main_classes/configuration"),
    "prometheus": ("Prometheus metric types", "https://prometheus.io/docs/concepts/metric_types/"),
    "kubernetes": ("Kubernetes GPU scheduling", "https://kubernetes.io/docs/tasks/manage-gpus/scheduling-gpus/"),
}


def L(
    no: int,
    slug: str,
    title: str,
    puzzle: str,
    hook: str,
    objects: str,
    mechanism: str,
    anchors: list[str],
    checks: list[str],
    experiment: str,
    baseline: str,
    candidate: str,
    controlled: str,
    metrics: str,
    code_walk: str,
    gate: str,
    failure: str,
    next_step: str,
    conclusion: str,
    evidence_label: str,
    result_fields: list[tuple[str, str, str]],
    refs: list[str],
    mermaid: str,
    steps: list[tuple[str, str]],
) -> dict[str, Any]:
    return locals()


LESSONS: list[dict[str, Any]] = [
    L(
        1, "inference-service-bottleneck", "The Inference Service Bottleneck",
        "Why can a fast single prompt still become an unreliable concurrent service?",
        "A model demo measures one request after warm-up. A service owns a queue, admission policy, cache budget, batching policy, and latency objective. The relevant question is not whether the model can generate text, but whether the serving layer can convert irregular arrivals into useful GPU work without sacrificing tail latency.",
        "The lab uses the installed vLLM engine, a local Qwen2.5 checkpoint, four prompts, generated-token counts, elapsed wall time, and CUDA memory observations. It records one native batch instead of inferring service behavior from a framework name.",
        "Autoregressive inference alternates a compute-heavy prompt pass with repeated one-token steps. Independent requests reach those phases at different times. A serving engine can schedule ready token steps together and manage their KV state, but no scheduler can remove model work or guarantee an SLO without an arrival model. Throughput, latency, and queueing therefore form separate evidence axes.",
        ["Single-request latency is not a concurrency result.", "Scheduling creates opportunities; workload shape determines whether they exist.", "A service decision needs both useful-token throughput and a tail-latency gate."],
        ["Predict the output-token throughput for four short prompts.", "Name one conclusion that an offline batch cannot establish.", "Choose the additional trace needed before setting an online SLO."],
        "Run one real vLLM offline batch and retain engine version, token counts, wall time, and GPU memory.",
        "one frozen Qwen2.5 model and four independent prompts", "vLLM native offline batching for those prompts", "model path, sampling, prompt set, maximum output, seed, and GPU", "requests, prompt/output tokens, elapsed seconds, output tokens/s, and memory",
        "The experiment loads the checkpoint once, generates all requests in one call, and reads token IDs from each RequestOutput. It deliberately reports batch elapsed time rather than inventing per-request TTFT from an offline API.",
        "Adopt vLLM as the measured serving candidate only after online arrival tests also satisfy TTFT, ITL, error-rate, and memory gates.",
        "Warm-up, compilation, prompt length, sampling, and model size can reverse the observed rate. An offline batch does not contain network, tokenizer, queue, or streaming latency.",
        "Replay a timestamped production-like arrival trace against the OpenAI endpoint and compare p50/p95 TTFT and ITL with the same model revision.",
        "The native run proves that this vLLM build serves the frozen batch on the RTX 5090; it does not prove a production concurrency SLO.",
        "native-backend",
        [("vLLM version", "vllm_version", "str"), ("Requests", "requests", "int"), ("Prompt tokens", "prompt_tokens", "int"), ("Output tokens", "output_tokens", "int"), ("Elapsed", "elapsed_s", "float"), ("Output throughput", "output_tokens_s", "rate"), ("Peak allocated", "peak_allocated_mib", "mib")],
        ["overview", "quickstart"],
        "flowchart LR\n  R[\"irregular requests\"] --> Q[\"waiting queue\"]\n  Q --> S[\"token scheduler\"]\n  S --> G[\"GPU model step\"]\n  G --> K[\"KV state\"]\n  G --> O[\"streamed tokens\"]\n  O --> E[\"TTFT + ITL + throughput evidence\"]",
        [("Separate the phases", "Treat prompt processing and token generation as different workloads."), ("Make requests schedulable", "Expose ready work to one engine rather than isolated model loops."), ("Measure useful work", "Count prompt and generated tokens beside elapsed time and memory."), ("Bound the claim", "Add online queueing evidence before promising a service objective.")],
    ),
    L(
        2, "prefill-decode-kv-cache", "Prefill, Decode, and the KV Cache",
        "Which phase owns TTFT, which phase owns ITL, and why does context remain resident?",
        "A long prompt and a long answer stress different parts of the engine. Collapsing them into one requests-per-second number hides whether attention over the prompt, repeated Decode steps, or KV capacity is the limiting factor.",
        "The experiment creates short- and long-prompt requests with short and long output limits, runs them through one native engine, and retains request metrics when exposed by the installed API. Token counts provide the fallback ledger.",
        "Prefill maps all prompt tokens through the model and materializes key/value vectors for every layer. Decode reuses that state and appends one position per step. For a standard decoder, KV bytes scale approximately with `2 × layers × tokens × kv_heads × head_dim × bytes_per_element`. TTFT contains queue plus prompt work; ITL reflects the sequence of Decode scheduling and execution events.",
        ["Prompt length primarily changes the initial compute and cache allocation.", "Output length repeats Decode and grows the cache one token at a time.", "Aggregate elapsed time cannot identify TTFT without first-token timing."],
        ["Rank the four workload cells by expected elapsed time.", "Calculate which cells reserve the most KV positions.", "State whether offline RequestOutput metrics expose true network-observed TTFT."],
        "Measure a 2×2 prompt/output grid through vLLM and retain token and request timing fields.",
        "short prompt with an eight-token answer", "long prompt and/or a 32-token answer", "engine, model, dtype, seed, sampling mode, and GPU", "prompt tokens, output tokens, elapsed time, and available request metrics",
        "Each workload runs as a separate native request after one warm-up. The code introspects the metrics object instead of assuming version-specific attributes, so absent fields remain explicit rather than fabricated.",
        "Use phase-specific optimization only after the metric that corresponds to that phase moves under a representative workload.",
        "Wall-clock measurements include Python and scheduler overhead. Repeated prose may tokenize differently than expected, and an offline first-token timestamp is not client-observed streaming latency.",
        "Run the same grid over the streaming API, timestamp every chunk at the client, and compare engine timestamps with network observations.",
        "Prefill, Decode, and KV growth are distinct mechanisms; this run measures their combined native request cost and exposes only the timing fields the API actually returns.",
        "native-backend",
        [("Short/short elapsed", "cases.short_short.elapsed_s", "float"), ("Long/short elapsed", "cases.long_short.elapsed_s", "float"), ("Short/long elapsed", "cases.short_long.elapsed_s", "float"), ("Long/long elapsed", "cases.long_long.elapsed_s", "float"), ("Longest prompt tokens", "cases.long_short.prompt_tokens", "int"), ("Longest output tokens", "cases.short_long.output_tokens", "int")],
        ["paged", "metrics"],
        "flowchart LR\n  P[\"prompt tokens\"] --> F[\"Prefill: many positions\"]\n  F --> K[\"layer KV cache\"]\n  K --> D[\"Decode: one new position\"]\n  D --> K\n  D --> T[\"next token\"]\n  F -.-> A[\"TTFT path\"]\n  D -.-> I[\"ITL path\"]",
        [("Tokenize first", "The prompt-token count defines initial work and cache positions."), ("Materialize reusable state", "Prefill writes keys and values for every layer."), ("Append during Decode", "Each generated token extends that state and triggers another model step."), ("Attach the right metric", "Use TTFT for initial work and ITL for repeated generation.")],
    ),
    L(
        3, "pagedattention-block-tables", "PagedAttention and Block Tables",
        "How can non-contiguous KV blocks reduce waste without changing attention semantics?",
        "Requests end at unpredictable lengths. Reserving one contiguous maximum-length slab per request strands memory, while moving live cache entries to repair fragmentation is expensive. PagedAttention introduces an indirection that lets logical token positions map to physical blocks.",
        "The notebook runs a deterministic allocator model for a mixed-length batch. It compares maximum-length slabs, exact variable allocations, and fixed-size blocks, then verifies that a shuffled block table reconstructs the same logical sequence.",
        "For block size `B`, a request of length `L` owns `ceil(L/B)` blocks and wastes fewer than `B` token slots. The block table maps each logical block number to a physical block. Attention follows that mapping when reading keys and values; physical adjacency is unnecessary. This changes allocation and addressing, not the mathematical attention weights.",
        ["Internal fragmentation is bounded by one partial block per sequence.", "External fragmentation is handled by assigning any free physical block.", "Block tables preserve logical order even when physical IDs are shuffled."],
        ["Compute slab waste for the supplied length distribution.", "Predict how block size changes waste and metadata.", "Verify reconstruction after random physical placement."],
        "Simulate slab and block allocation, then reconstruct logical token IDs through a randomized block table.",
        "one max-sequence contiguous reservation per request", "fixed-size paged blocks assigned from a shared pool", "request lengths, element footprint, seed, and logical payload", "reserved token slots, waste ratio, block count, and reconstruction equality",
        "The code treats every token slot as a visible integer, places blocks at non-contiguous physical IDs, and gathers them through the table. This makes address translation inspectable without claiming a CUDA kernel trace.",
        "Select a block size only after both fragmentation and scheduler/kernel constraints are evaluated on the target distribution.",
        "The allocator model omits copy-on-write, prefix sharing, eviction, block metadata bytes, alignment, and kernel execution. It teaches the invariant but cannot predict native latency.",
        "Collect live request lengths and engine cache metrics, sweep supported block sizes, and add prefix-cache sharing plus eviction events.",
        "Paged blocks bound per-request tail waste and allow non-contiguous placement; the numerical reconstruction proves the mapping invariant, not native PagedAttention speed.",
        "numerical-model",
        [("Slab reserved tokens", "slab_reserved_tokens", "int"), ("Paged reserved tokens", "paged_reserved_tokens", "int"), ("Slab waste", "slab_waste_ratio", "percent"), ("Paged waste", "paged_waste_ratio", "percent"), ("Physical blocks", "physical_blocks", "int"), ("Reconstruction exact", "reconstruction_exact", "bool")],
        ["paged", "engine_args"],
        "flowchart LR\n  L0[\"logical block 0\"] -->|table| P7[\"physical block 7\"]\n  L1[\"logical block 1\"] -->|table| P2[\"physical block 2\"]\n  L2[\"logical block 2\"] -->|table| P9[\"physical block 9\"]\n  P7 --> A[\"attention reads logical order\"]\n  P2 --> A\n  P9 --> A",
        [("Partition logical positions", "Group token positions into equal-size logical blocks."), ("Allocate from a pool", "Assign each logical block any available physical block."), ("Follow the table", "Gather physical blocks in logical order inside attention."), ("Account for the tail", "Only the last block of each request can be partially unused.")],
    ),
    L(
        4, "continuous-batching", "Continuous Batching, Throughput, and Fairness",
        "When should a newly arrived short request enter a GPU batch?",
        "Static batching waits for every sequence in a group to finish. Autoregressive sequences rarely finish together, so completed rows become empty work while new arrivals remain queued. Iteration-level scheduling can refill those slots, but an unconstrained throughput policy can starve old or large requests.",
        "A discrete-event scheduler replays the same arrivals and token demands under static groups, shortest-remaining-token priority, and age-aware continuous batching. It retains completion time for every request.",
        "At each Decode tick, continuous batching chooses up to `C` active sequences, advances each by one token, releases completed sequences, and admits more work. Shortest-remaining-token priority lowers average latency but can postpone long jobs. Adding an age term or service class changes the objective from pure token throughput to a declared fairness policy.",
        ["Batch membership can change after every model step.", "The same token capacity can produce different tail latency under different priorities.", "Fairness must be encoded as a scheduling rule and measured per request."],
        ["Predict which scheduler minimizes makespan.", "Identify which policy hurts the oldest long request.", "Choose an observable starvation gate."],
        "Replay one arrival trace through three schedulers and compare makespan, p95 completion, and maximum wait.",
        "static groups that drain before admitting new work", "continuous shortest-remaining and age-aware scheduling", "arrival trace, per-request tokens, tick capacity, and tie-breaking", "makespan, mean/p95 latency, maximum wait, and completion order",
        "The simulation operates on explicit token quanta and keeps a per-request timeline. Its purpose is to make policy consequences visible; it does not substitute for native scheduler profiling.",
        "Choose the policy whose tail and starvation metrics meet service-class gates at an acceptable throughput cost.",
        "Real Prefill steps have unequal cost, CUDA batches are not constant-duration ticks, and memory pressure can block admission. The trace is illustrative rather than a capacity prediction.",
        "Replay production arrival timestamps with prompt lengths, streaming duration, priority class, cancellations, and measured per-step costs from the real engine.",
        "Continuous batching can reclaim finished slots immediately, while the priority rule—not batching alone—determines fairness.",
        "numerical-model",
        [("Static makespan", "static.makespan", "float"), ("Continuous makespan", "shortest.makespan", "float"), ("Age-aware makespan", "age_aware.makespan", "float"), ("Static p95", "static.p95_latency", "float"), ("Shortest max wait", "shortest.max_wait", "float"), ("Age-aware max wait", "age_aware.max_wait", "float")],
        ["overview", "paged"],
        "flowchart TD\n  A[\"request arrivals\"] --> Q[\"waiting queue\"]\n  Q --> P[\"priority + admission\"]\n  P --> B[\"active token batch\"]\n  B --> G[\"one model step\"]\n  G --> C{\"request complete?\"}\n  C -->|\"no\"| P\n  C -->|\"yes\"| O[\"release slot and KV blocks\"]",
        [("Observe arrivals", "Record when each request becomes eligible."), ("Apply a named priority", "Select active work using remaining tokens, age, or class."), ("Advance one iteration", "Generate one token for each scheduled sequence."), ("Measure individuals", "Retain completion and waiting time instead of only aggregate tokens.")],
    ),
    L(
        5, "kv-memory-budget", "A KV-Cache Memory Budget",
        "How many concurrent long-context requests fit after model weights and runtime reserve?",
        "A model can load successfully and still fail when context accumulates. Capacity planning must reserve space for weights, runtime workspace, non-torch allocations, and uncertainty before assigning the remainder to KV cache.",
        "The notebook reads the local model configuration and real GPU memory, derives KV bytes per token from layer/head geometry, and computes conservative concurrency for several context lengths and cache dtypes.",
        "For grouped-query attention, one token stores keys and values for `num_key_value_heads`, not all query heads. A first-order decoder cache uses `2 × layers × kv_heads × head_dim × element_bytes` per token. Dividing a declared KV budget by that footprint gives token capacity; dividing again by context length gives only a theoretical concurrency ceiling.",
        ["KV geometry comes from the model config.", "Context and concurrency multiply the token footprint.", "A safe budget subtracts weights, workspace, and headroom before division."],
        ["Derive BF16 KV bytes per token for the local checkpoint.", "Predict the capacity change from BF16 to FP8 cache.", "State why theoretical concurrency exceeds an operational limit."],
        "Combine measured GPU memory with model geometry and a declared reserve to calculate context/concurrency cells.",
        "BF16 KV storage under one fixed memory budget", "FP8 KV storage and multiple context lengths", "model config, GPU total, weight estimate, reserve fraction, and utilization cap", "KV bytes/token, token capacity, and theoretical concurrent sequences",
        "The code parses only local configuration fields, shows every subtraction, and emits the full capacity table. No hidden allocator efficiency is inserted into the result.",
        "Set admission limits below the calculated ceiling and validate them with native load, fragmentation, and tail-latency tests.",
        "Sliding-window attention, hybrid state-space layers, cache alignment, CUDA graphs, prefix sharing, and engine reservations can change the native allocation. Model-file bytes are not identical to resident weight memory.",
        "Start the engine at selected utilization limits, issue long-context concurrency sweeps, and reconcile engine cache-block metrics with the first-order ledger.",
        "KV capacity is a budget equation anchored in model geometry; its result is a planning ceiling until native concurrency tests pass.",
        "capacity-model",
        [("GPU total", "gpu_total_mib", "mib"), ("BF16 KV bytes/token", "kv_bytes_per_token.bf16", "bytes"), ("FP8 KV bytes/token", "kv_bytes_per_token.fp8", "bytes"), ("BF16 token capacity", "token_capacity.bf16", "int"), ("FP8 token capacity", "token_capacity.fp8", "int"), ("BF16 8K concurrency", "concurrency.bf16.8192", "int")],
        ["engine_args", "kv_quant"],
        "flowchart LR\n  G[\"GPU memory\"] --> S[\"subtract weights\"]\n  S --> W[\"subtract workspace + headroom\"]\n  W --> K[\"KV budget\"]\n  M[\"layers × KV heads × head dim × dtype\"] --> B[\"bytes per token\"]\n  K --> C[\"token capacity\"]\n  B --> C\n  C --> R[\"context × concurrency ceiling\"]",
        [("Read model geometry", "Use KV heads and head dimension, not parameter count alone."), ("Declare non-KV reserves", "Weights and workspace leave only part of VRAM for context state."), ("Calculate token capacity", "Divide available bytes by the per-token footprint."), ("Validate below the ceiling", "Native allocation and latency tests determine the operational limit.")],
    ),
    L(
        6, "installation-compatibility", "Installing a Reproducible vLLM Environment",
        "What evidence shows that Python, PyTorch, CUDA, the driver, and vLLM agree?",
        "A successful package installation is not a successful GPU runtime. vLLM ships compiled components tied to platform and PyTorch choices, so the environment record must include imports, binary version, CUDA availability, GPU capability, and a minimal native operation.",
        "The compatibility probe imports vLLM and PyTorch, captures exact versions, locates the CLI, inspects selected engine arguments, and executes a CUDA tensor operation. It records missing features as data.",
        "The NVIDIA driver provides the kernel-facing CUDA capability; a PyTorch wheel carries its CUDA runtime; vLLM adds compiled extensions and generated kernels. These versions need not have identical labels, but the installed combination must support the GPU architecture and import without unresolved symbols. A clean environment prevents unrelated packages from silently replacing that combination.",
        ["Driver, wheel runtime, and compiler toolkit are different version fields.", "Import success is weaker than native engine execution.", "A pinned environment is part of every benchmark identity."],
        ["Predict the compute capability and CUDA runtime reported remotely.", "Check whether the vLLM CLI exposes serve and bench.", "Name the minimal step beyond import needed for release confidence."],
        "Collect the complete stack identity and run a real CUDA sanity operation inside the isolated environment.",
        "package metadata alone", "imports, CLI surface, compiled extension visibility, and CUDA execution", "isolated environment and one RTX 5090", "versions, executable paths, CLI subcommands, tensor checksum, and feature flags",
        "The probe avoids network downloads and shell-specific environment assumptions. Every field comes from the Python process that will execute the remaining labs.",
        "Proceed to model experiments only when the pinned interpreter imports the stack, sees the GPU, and completes a CUDA operation.",
        "A small tensor operation exercises PyTorch rather than every vLLM kernel. Later model loads can still fail because of architecture, dtype, memory, or compilation issues.",
        "Archive `pip freeze`, vLLM collect-env output, driver information, model hash, and one completed model-generation artifact with the release.",
        "Compatibility is a chain of executable checks; this probe establishes the local stack identity and CUDA path, not every model feature.",
        "compatibility-probe",
        [("vLLM version", "vllm_version", "str"), ("Python", "python", "str"), ("PyTorch", "torch", "str"), ("CUDA runtime", "cuda_runtime", "str"), ("CLI found", "cli_found", "bool"), ("Serve command", "serve_command", "bool"), ("CUDA checksum", "cuda_checksum", "float")],
        ["install", "engine_args"],
        "flowchart LR\n  D[\"NVIDIA driver\"] --> T[\"PyTorch CUDA runtime\"]\n  T --> V[\"vLLM compiled + Python package\"]\n  V --> M[\"model architecture + dtype\"]\n  M --> R[\"native generation\"]\n  R --> A[\"reproducible environment artifact\"]",
        [("Pin the interpreter", "Create an isolated Python environment."), ("Install one coherent stack", "Let the selected vLLM wheel resolve its compatible PyTorch build."), ("Probe the executable path", "Verify imports, CLI, GPU identity, and a CUDA operation."), ("Prove model execution", "Treat later native generation as the final compatibility link.")],
    ),
    L(
        7, "offline-llm-api", "Offline Inference with LLM and SamplingParams",
        "What is the smallest reproducible vLLM generation program?",
        "Offline inference removes HTTP and queueing from the first functional test. It is the right place to pin model identity, tokenization, sampling, request ordering, and output serialization before diagnosing a service layer.",
        "The notebook initializes `LLM`, builds explicit `SamplingParams`, submits three prompts in one call, and saves prompt/output token IDs, finish reasons, text hashes, and elapsed time.",
        "`LLM.generate` accepts a batch of prompts and schedules them through the engine. `SamplingParams` is part of the output contract: temperature, top-p, stop rules, maximum tokens, seed, and logprobs can all change observable results. Reproducibility therefore requires both engine and request configuration.",
        ["Model configuration and sampling configuration are separate inputs.", "Token IDs are a more stable audit artifact than rendered whitespace alone.", "Offline generation validates the model path without measuring HTTP behavior."],
        ["Predict the finish reason under a small maximum-token limit.", "Identify which request fields must be pinned.", "Explain why a text hash does not establish semantic quality."],
        "Generate a three-prompt batch through the native offline API and retain a structured request/output record.",
        "implicit defaults and printed text", "explicit SamplingParams and token-level artifacts", "model, tokenizer, prompts, seed, maximum tokens, and GPU", "token counts, finish reasons, output hashes, elapsed time, and throughput",
        "The code uses greedy decoding to make the first path easy to audit. It reads each output object rather than parsing console logs and keeps only compact hashes plus short previews in the artifact.",
        "Treat the offline path as ready when all requests finish, identities match, and reruns satisfy the declared token-level tolerance.",
        "GPU reductions and batching can introduce small numerical differences across versions. Greedy output can diverge after a near-tie, and token equality does not prove task correctness.",
        "Add task-specific evaluation, multiple prompt lengths, a streaming server comparison, and model/tokenizer revision hashes.",
        "An explicit offline program is the functional baseline for later service experiments; it proves native generation, not online performance.",
        "native-backend",
        [("Requests", "requests", "int"), ("Prompt tokens", "prompt_tokens", "int"), ("Output tokens", "output_tokens", "int"), ("Elapsed", "elapsed_s", "float"), ("Throughput", "output_tokens_s", "rate"), ("Unique output hashes", "unique_output_hashes", "int")],
        ["quickstart", "sampling"],
        "flowchart LR\n  M[\"model path + engine args\"] --> L[\"LLM engine\"]\n  P[\"prompts\"] --> G[\"generate\"]\n  S[\"SamplingParams\"] --> G\n  L --> G\n  G --> O[\"RequestOutput objects\"]\n  O --> A[\"tokens + finish reason + hashes\"]",
        [("Freeze engine identity", "Pin model path, dtype, maximum length, and vLLM version."), ("Make sampling explicit", "Avoid defaults that can change with a release."), ("Read structured outputs", "Retain token IDs and finish reasons from the API object."), ("Add quality separately", "Functional generation is only the first acceptance layer.")],
    ),
    L(
        8, "openai-compatible-service", "The OpenAI-Compatible HTTP Service",
        "Does API compatibility mean every endpoint and field behaves identically?",
        "A compatible endpoint lowers client migration cost, but it does not erase model capabilities, server-specific fields, parser requirements, or release differences. The contract must be tested against the exact server build and model.",
        "The lab launches `vllm serve` as a child process, waits for readiness, calls `/v1/models` and `/v1/chat/completions`, captures status and timing, then terminates the server cleanly. Unsupported probes remain explicit.",
        "The server translates an HTTP request into tokenizer, scheduler, sampling, and streaming operations. Compatibility is endpoint- and field-level: a model may support Chat but not embeddings, a tool parser may require flags, and extra vLLM parameters can extend the schema. Readiness, request success, and response structure are separate checks.",
        ["HTTP 200 does not imply semantic correctness.", "Endpoint availability depends on model task and server configuration.", "Server startup, request latency, and engine execution need separate evidence."],
        ["Predict which endpoint will identify the served model.", "List required Chat response fields.", "Name one OpenAI feature that must be probed rather than assumed."],
        "Start the real server on localhost, issue model and Chat requests, validate their JSON shape, and preserve a log tail.",
        "offline generation only", "localhost OpenAI-compatible HTTP serving", "model, port, sampling, prompt, timeout, and server arguments", "startup time, status codes, response schema, token usage, request latency, and shutdown",
        "The subprocess receives an argument list rather than a shell command. The code polls readiness with a deadline, records a bounded log tail, and always terminates the process in a `finally` block.",
        "Enable a client route only when required endpoints, fields, streaming behavior, errors, and authentication controls pass contract tests.",
        "Loopback tests exclude proxies, TLS, network jitter, load balancing, and multi-tenant controls. A single response cannot validate all compatibility or parser combinations.",
        "Run a versioned contract suite for Chat, Responses, embeddings, streaming, errors, cancellation, tools, and usage accounting through the production gateway.",
        "The localhost test proves the selected Chat route and response schema for this model/server pair; compatibility beyond that matrix remains unmeasured.",
        "native-backend",
        [("Server ready", "server_ready", "bool"), ("Startup", "startup_s", "float"), ("Models status", "models_status", "int"), ("Chat status", "chat_status", "int"), ("Chat latency", "chat_latency_s", "float"), ("Completion tokens", "completion_tokens", "int"), ("Schema valid", "schema_valid", "bool")],
        ["openai", "quickstart"],
        "sequenceDiagram\n  participant C as Client\n  participant A as API server\n  participant E as vLLM engine\n  C->>A: GET /v1/models\n  A-->>C: served model identity\n  C->>A: POST /v1/chat/completions\n  A->>E: tokenize + schedule\n  E-->>A: generated token stream\n  A-->>C: compatible JSON response",
        [("Wait for readiness", "Do not mix server startup time with request failure."), ("Probe model identity", "Confirm which name clients must send."), ("Validate required fields", "Check choices, message content, finish reason, and usage."), ("Test the production path", "Repeat through authentication, TLS, gateway, and streaming layers.")],
    ),
    L(
        9, "sampling-output-control", "Sampling and Output Control",
        "Which parameter changed the answer when two requests used the same model?",
        "Sampling parameters are part of the public API, not harmless presentation options. Temperature changes the logit scale; top-p truncates the candidate mass; stop rules can remove suffixes; logprobs change response volume and observability.",
        "One native engine executes greedy, seeded stochastic, top-p, stop-string, and logprob cases. The result records token hashes and selected logprob metadata without treating variation as model quality.",
        "Greedy decoding selects the maximum logit. Temperature divides logits before normalization, while nucleus sampling keeps the smallest set whose cumulative probability reaches `top_p`. A seed scopes pseudo-random choices, but numerical and scheduling changes can still affect close probabilities. Stop conditions terminate generation after a matched token or string according to API semantics.",
        ["Temperature and top-p act at different stages of sampling.", "A fixed seed is necessary but not a cross-version guarantee.", "Stop rules affect both visible text and finish metadata."],
        ["Predict which cases are deterministic within one run.", "Identify whether stop text appears in output.", "State the extra payload cost of logprobs."],
        "Run five explicit SamplingParams cases and compare token sequences, finish reasons, and logprob availability.",
        "greedy decoding with no stop rule", "seeded sampling, top-p, stop, and logprob variants", "model, prompt, maximum tokens, engine, and GPU", "token hashes, token counts, finish reasons, stop inclusion, and logprob presence",
        "The code constructs a fresh SamplingParams object for every case and stores the effective settings next to its output. It never labels a different sample as better without an evaluator.",
        "Promote an API configuration only after deterministic, stochastic, stop, and observability cases match the product contract.",
        "Tokenizer boundaries can make a string stop behave differently from a token stop. Logprob structures and reproducibility guarantees can change across versions.",
        "Add streaming stops, bad-word filters, min-p, repetition controls, and statistical distribution checks over many seeds.",
        "Sampling parameters define observable behavior; the experiment localizes output changes to explicit request configurations.",
        "native-backend",
        [("Cases", "cases", "int"), ("Unique token hashes", "unique_hashes", "int"), ("Greedy tokens", "greedy.output_tokens", "int"), ("Sampled tokens", "sampled.output_tokens", "int"), ("Stop finish reason", "stop.finish_reason", "str"), ("Logprobs returned", "logprobs.returned", "bool")],
        ["sampling", "openai"],
        "flowchart LR\n  L[\"model logits\"] --> T[\"temperature scaling\"]\n  T --> P[\"top-p candidate set\"]\n  P --> R[\"seeded random draw\"]\n  R --> S{\"stop matched?\"}\n  S -->|\"no\"| L\n  S -->|\"yes\"| O[\"text + finish reason + optional logprobs\"]",
        [("Freeze raw request settings", "Keep every sampling field beside the output."), ("Separate selection stages", "Temperature rescales; top-p filters; the RNG draws."), ("Inspect termination", "Read stop behavior and finish reason together."), ("Evaluate quality elsewhere", "Variation is not evidence of improvement.")],
    ),
    L(
        10, "model-loading-provenance", "Model Loading, Formats, and Provenance",
        "Can a model name reproduce a deployment after its remote repository changes?",
        "A serving manifest must identify weight files, configuration, tokenizer, code trust, and revisions. A convenient repository name is mutable unless resolved to immutable content.",
        "The lab audits the local checkpoint without network access: required files, safetensors header, configuration fields, tokenizer metadata, file sizes, and SHA-256 digests. It then confirms native loading in the pinned engine.",
        "vLLM combines a model configuration, tokenizer, weight loader, architecture implementation, and optional remote code. Safetensors avoids pickle execution but does not establish model license or semantic identity. Content hashes make local bytes immutable; upstream commit revisions make remote retrieval repeatable. `trust_remote_code` expands the executable trust boundary and must be an explicit decision.",
        ["Model weights and tokenizer are separate versioned artifacts.", "A format safety property is not a provenance record.", "Remote code changes the supply-chain boundary."],
        ["List the files required by this local checkpoint.", "Predict whether one or multiple safetensors shards exist.", "Choose the immutable identifiers for a release manifest."],
        "Hash the local model/config/tokenizer artifacts, inspect format metadata, and perform a native load/generation check.",
        "a mutable model name with implicit defaults", "a content-addressed local manifest plus native load", "checkpoint path, file bytes, offline mode, engine arguments, and prompt", "hashes, sizes, architecture, dtype, tokenizer class, trust setting, and load success",
        "The code reads JSON and safetensors metadata without deserializing arbitrary Python objects. Hashes are streamed so the 3 GB weight file does not enter host memory at once.",
        "Release only when model, tokenizer, config, code trust, license review, and native load are bound to immutable identifiers.",
        "Local hashes cannot reveal the upstream commit if the directory lost repository metadata. A successful generation does not validate the license, training provenance, or every architecture feature.",
        "Resolve the upstream commit, sign the manifest, verify it in image build and startup, and test a representative prompt suite after every loader change.",
        "Reproducible loading requires immutable bytes and explicit trust decisions; a model alias alone is insufficient.",
        "native-backend",
        [("Weight files", "weight_files", "int"), ("Weight bytes", "weight_bytes", "bytes"), ("Config hash", "hashes.config", "short_hash"), ("Tokenizer hash", "hashes.tokenizer", "short_hash"), ("Weight hash", "hashes.weights", "short_hash"), ("Architecture", "architecture", "str"), ("Native load", "native_load", "bool")],
        ["hf_config", "models"],
        "flowchart LR\n  R[\"upstream revision\"] --> M[\"local manifest\"]\n  W[\"safetensors bytes\"] --> M\n  C[\"config + tokenizer\"] --> M\n  T[\"remote-code trust decision\"] --> M\n  M --> L[\"vLLM loader\"]\n  L --> E[\"generation + signed evidence\"]",
        [("Inventory artifacts", "Separate weights, config, tokenizer, and optional code."), ("Resolve immutable identities", "Use commit revisions and content hashes."), ("Declare trust", "Make remote-code and license decisions visible."), ("Execute the manifest", "Prove the exact bytes load in the target engine.")],
    ),
    L(
        11, "multi-gpu-parallelism", "Tensor, Pipeline, Data, and Expert Parallelism",
        "How should a 70B service map onto eight GPUs when one RTX 5090 cannot reproduce that topology?",
        "Parallelism is a placement decision constrained by model size, communication, request isolation, and cluster topology. Choosing `tensor_parallel_size=8` because eight devices exist can place collective traffic across a slow boundary and reduce useful throughput.",
        "The lab records the real single-GPU environment, reads the installed vLLM parallel CLI surface, and evaluates a transparent eight-GPU placement model for TP, PP, DP, and hybrid layouts across two four-GPU nodes.",
        "Tensor parallelism shards layer operations and communicates on many layers. Pipeline parallelism assigns layer stages and introduces bubbles or microbatch scheduling. Data parallel replicas own separate request batches and normally duplicate weights. Expert parallelism shards routed experts while preserving dense/shared components. Communication frequency and link bandwidth must be matched to the topology.",
        ["Model fit is a hard constraint before throughput optimization.", "TP communication happens inside the model step and is topology-sensitive.", "DP increases replica concurrency only when each replica can fit the model."],
        ["Eliminate layouts that cannot fit 70B BF16 weights.", "Compare estimated cross-node bytes for TP8 and TP4×DP2.", "Name the NCCL trace required before a native claim."],
        "Evaluate candidate placements with explicit weight, KV, link, and collective assumptions; probe available engine arguments.",
        "TP8 spanning two nodes", "TP4 within each node plus DP2, and PP alternatives", "eight-GPU topology, model bytes, per-GPU memory, link assumptions, and batch", "fit, replica count, cross-node communication estimate, and exposed CLI flags",
        "Every formula and assumed bandwidth is emitted in the artifact. The experiment does not initialize distributed processes, so all multi-GPU performance rows remain planning estimates.",
        "Select only layouts that fit with headroom, keep frequent collectives on fast links, and then pass a native multi-node benchmark.",
        "Collective algorithms, overlap, quantized weights, expert routing, uneven layers, and scheduler behavior can dominate the simplified estimate. One RTX 5090 cannot validate NCCL topology.",
        "Run the selected two-node layout with NCCL traces, per-rank memory, failure injection, and identical request replay; compare against the best single-node baseline.",
        "The capacity model rejects impossible or topology-hostile layouts; it does not measure multi-GPU vLLM performance.",
        "capacity-model",
        [("GPU count", "topology.gpus", "int"), ("GPUs per node", "topology.gpus_per_node", "int"), ("TP8 fits", "layouts.tp8.fits", "bool"), ("TP4-DP2 fits", "layouts.tp4_dp2.fits", "bool"), ("TP8 cross-node bytes", "layouts.tp8.cross_node_gib_step", "float"), ("TP4-DP2 replicas", "layouts.tp4_dp2.replicas", "int"), ("TP flag available", "cli.tensor_parallel", "bool")],
        ["parallel", "engine_args"],
        "flowchart TD\n  M[\"model + KV memory\"] --> F{\"fits one GPU?\"}\n  F -->|\"yes\"| D[\"data-parallel replicas\"]\n  F -->|\"no\"| T[\"tensor or pipeline shards\"]\n  T --> N{\"fast links within node?\"}\n  N -->|\"yes\"| H[\"TP inside node + DP across nodes\"]\n  N -->|\"no\"| P[\"revisit PP / quantization / capacity\"]",
        [("Solve memory fit", "Remove layouts that cannot hold weights, KV, and headroom."), ("Map communication", "Mark which collectives cross NVLink, PCIe, or the network."), ("Choose replication", "Use DP only after a complete replica fits."), ("Prove natively", "Collect per-rank traces and throughput on the actual topology.")],
    ),
    L(
        12, "automatic-prefix-caching", "Automatic Prefix Caching",
        "When can a shared system prompt skip Prefill work, and when is the cache key different?",
        "Chat, document analysis, and few-shot workloads often repeat a long prefix. Automatic prefix caching can reuse KV blocks for exactly matching token prefixes, but it cannot reuse new suffix computation and it is not a semantic similarity cache.",
        "A native engine with prefix caching enabled serves a cold shared prefix, a warm exact prefix, and a one-token-mutated prefix. The lab retains cached-token fields exposed by RequestOutput and elapsed time for each case.",
        "A cache key covers token content and additional factors that affect KV validity. Matching full blocks can be referenced by another request; the final partial block and new suffix still require work. Hash lookup changes scheduling cost but not output semantics. Eviction and cache capacity determine whether a theoretical hit remains resident.",
        ["Reuse requires token-exact prefix identity.", "Only cached Prefill blocks are skipped; Decode is unchanged.", "A hit-rate metric needs a request distribution and eviction window."],
        ["Predict which request reports cached tokens.", "Explain why changing one early token destroys downstream prefix matches.", "Choose a workload where APC should not be enabled solely for speed."],
        "Run cold, warm-exact, and mutated-prefix requests through one APC-enabled native engine.",
        "cold shared prefix", "warm exact reuse and a near-match control", "engine instance, prefix length, suffix, sampling, maximum output, and GPU", "cached tokens, prompt tokens, elapsed time, output identity, and cache configuration",
        "The code keeps one engine alive so the second request can reuse resident blocks. It introspects cache-related metrics and records `None` when the installed API does not expose a field.",
        "Enable APC for a route only when production prefixes repeat, correctness is unchanged, and hit-rate/latency improve without harmful cache pressure.",
        "Very short prefixes, low repetition, eviction, multimodal hashes, LoRA identity, or non-deterministic prompt construction can eliminate reuse. Elapsed time alone does not prove a cache hit.",
        "Instrument prefix-cache query/hit metrics under a real trace, then segment results by prefix family, block alignment, eviction age, and tenant boundary.",
        "APC reuses exact, valid KV blocks; the retained native metadata distinguishes an observed hit from a timing guess.",
        "native-backend",
        [("Cold cached tokens", "cold.cached_tokens", "int"), ("Warm cached tokens", "warm.cached_tokens", "int"), ("Mutated cached tokens", "mutated.cached_tokens", "int"), ("Cold elapsed", "cold.elapsed_s", "float"), ("Warm elapsed", "warm.elapsed_s", "float"), ("Warm output tokens", "warm.output_tokens", "int")],
        ["apc", "paged"],
        "flowchart LR\n  P[\"tokenized prefix\"] --> H[\"block hash lookup\"]\n  H --> M{\"valid block match?\"}\n  M -->|\"yes\"| R[\"reference cached KV blocks\"]\n  M -->|\"no\"| C[\"compute Prefill blocks\"]\n  R --> S[\"compute new suffix\"]\n  C --> S\n  S --> D[\"Decode normally\"]",
        [("Tokenize deterministically", "Cache identity starts from exact token blocks."), ("Look up full blocks", "Only valid resident matches can be referenced."), ("Compute the remainder", "Unmatched suffix and partial blocks still run Prefill."), ("Measure hit value", "Pair cached-token counters with latency and eviction behavior.")],
    ),
    L(
        13, "chunked-prefill", "Chunked Prefill and Decode Interference",
        "Should a long prompt monopolize one scheduling iteration while short requests wait?",
        "Large Prefill batches improve compute utilization but can delay active Decode sequences. Chunking divides prompt work into token budgets so the scheduler can interleave it with latency-sensitive generation.",
        "The lab replays a measured-cost scheduling model and probes installed chunked-prefill arguments. It compares an unchunked 4096-token prompt with 512-token chunks while short Decode jobs arrive.",
        "A scheduler with maximum batched-token budget `B` can consume a prompt in `ceil(L/B)` chunks. Smaller chunks create more scheduling opportunities for Decode but may add overhead and reduce Prefill efficiency. The correct chunk size is therefore an SLO trade-off, not a universal minimum.",
        ["Chunking changes when work is scheduled, not total prompt tokens.", "Smaller chunks can lower blocking time while adding launch/scheduling overhead.", "TTFT and ITL may move in opposite directions."],
        ["Predict short-request maximum delay without chunking.", "Calculate the number of 512-token chunks.", "Name the native trace needed to choose the budget."],
        "Interleave one long Prefill with short Decode jobs in a cost-calibrated scheduler model and inspect CLI support.",
        "one monolithic long Prefill", "fixed-size chunked Prefill interleaved with Decode", "token demands, per-token cost assumptions, arrival times, priority, and GPU identity", "long TTFT proxy, short p95 delay, scheduling rounds, and CLI feature presence",
        "The simulation exposes its cost coefficients and full event timeline. It remains separate from native vLLM timing because one GPU kernel does not have constant per-token cost.",
        "Choose a chunk budget only when native mixed-traffic replay meets both long-prompt TTFT and active-request ITL gates.",
        "Attention kernels scale nonlinearly with sequence length; CUDA graphs, batching, prefix hits, and compilation change step duration. Model results are directional only.",
        "Sweep `max_num_batched_tokens` on the real engine with concurrent streaming clients and retain scheduler metrics, TTFT, ITL, throughput, and GPU utilization.",
        "Chunked Prefill creates scheduling opportunities; its production value must be selected from native mixed-traffic trade-offs.",
        "numerical-model",
        [("Unchunked short p95", "unchunked.short_p95_delay", "float"), ("Chunked short p95", "chunked.short_p95_delay", "float"), ("Unchunked long finish", "unchunked.long_finish", "float"), ("Chunked long finish", "chunked.long_finish", "float"), ("Chunks", "chunked.prefill_chunks", "int"), ("CLI support", "cli.chunked_prefill", "bool")],
        ["engine_args", "overview"],
        "gantt\n  title Mixed prompt and Decode work\n  dateFormat X\n  axisFormat %L\n  section Unchunked\n  Long Prefill :0, 8\n  Short Decode :8, 11\n  section Chunked\n  Prefill chunk 1 :0, 2\n  Short Decode :2, 3\n  Prefill chunk 2 :3, 5",
        [("Set a token budget", "Bound how much prompt work enters one scheduler iteration."), ("Split the long prompt", "Create multiple resumable Prefill chunks."), ("Admit Decode between chunks", "Give active requests opportunities to advance."), ("Sweep the trade-off", "Measure TTFT, ITL, and throughput together on the native engine.")],
    ),
    L(
        14, "weight-quantization-deployment", "Weight Quantization Deployment Contracts",
        "Why can an AWQ, GPTQ, or FP8 checkpoint fail even when vLLM supports that method?",
        "A quantization label is only one field of a deployment contract. GPU capability, weight layout, group size, activation dtype, model architecture, loader metadata, and kernel availability must agree.",
        "The compatibility lab inspects vLLM's registered quantization methods and engine CLI, reads the unquantized local checkpoint metadata, and evaluates a declared RTX 5090 compatibility matrix without downloading substitute models.",
        "Weight-only AWQ and GPTQ store codes plus scale metadata and depend on kernels that understand their packing. FP8 may target weights and/or activations with hardware-specific execution. A loader can recognize a format yet fall back, reject an architecture, or execute with no speedup at the tested shape. Native model artifacts are required for performance evidence.",
        ["Loader recognition is weaker than kernel dispatch.", "Hardware support does not validate checkpoint metadata.", "Memory, quality, and latency require separate gates."],
        ["Identify the local checkpoint's declared quantization config.", "Probe whether AWQ, GPTQ, and FP8 names are registered.", "State why no latency comparison is made in this lesson."],
        "Probe installed quantization registrations and evaluate checkpoint/hardware prerequisites for three deployment routes.",
        "local BF16 checkpoint", "AWQ, GPTQ, and FP8 candidate contracts", "vLLM build, GPU, local config, and no network download", "registered methods, checkpoint declaration, hardware capability, readiness fields, and missing evidence",
        "The notebook imports registries defensively because internal module paths can change. A failed probe is retained as compatibility evidence rather than converted into a success claim.",
        "Benchmark a quantized route only after its exact checkpoint loads, a native trace identifies the intended path, and output quality passes.",
        "Registry presence can outlive a deprecated path or omit platform-specific constraints. This lab has no AWQ/GPTQ/FP8 weight artifact and therefore cannot measure their memory or speed.",
        "Pin one quantized checkpoint per route, hash it, run the same prompt grid, capture kernel traces, and compare quality plus memory against BF16.",
        "This probe maps available software vocabulary and missing prerequisites; it deliberately makes no quantized-performance claim.",
        "compatibility-probe",
        [("Declared local quantization", "local.quantization", "str"), ("AWQ registered", "methods.awq", "bool"), ("GPTQ registered", "methods.gptq", "bool"), ("FP8 registered", "methods.fp8", "bool"), ("Compute capability", "hardware.compute_capability", "str"), ("Native quantized benchmark", "native_benchmark_completed", "bool")],
        ["quant", "engine_args"],
        "flowchart TD\n  C[\"quantized checkpoint metadata\"] --> L{\"loader supports format?\"}\n  H[\"GPU capability\"] --> K{\"native kernel available?\"}\n  L --> K\n  K --> E[\"execute frozen workload\"]\n  E --> G{\"quality + memory + latency gates\"}\n  G -->|\"pass\"| P[\"promote route\"]\n  G -->|\"fail\"| R[\"rollback to BF16\"]",
        [("Inspect the checkpoint", "Read format, group, scale, and architecture metadata."), ("Match the platform", "Verify the GPU and compiled kernel prerequisites."), ("Prove dispatch", "Use native logs or traces, not a config label."), ("Gate the product result", "Evaluate quality, memory, and service latency separately.")],
    ),
    L(
        15, "fp8-kv-cache", "FP8 KV Cache: Capacity and Fidelity",
        "Does halving KV element width double safe long-context concurrency without changing answers?",
        "KV quantization targets state that grows with context rather than model weights. It can materially extend capacity, but scale calibration, numerical drift, kernel support, and non-KV reserves prevent a free two-times service claim.",
        "The native experiment runs the same deterministic prompts with automatic and FP8 KV cache dtypes, records success or the exact failure, compares token IDs, and pairs the result with the first-order byte ratio.",
        "FP8 stores each cached key/value element in one byte rather than BF16's two, plus scale metadata. Attention must dequantize or consume that representation through a supported path. Static or dynamic scales determine range and error. Even perfect twofold KV compression does not halve weight or workspace memory.",
        ["KV dtype is independent of weight dtype.", "Theoretical KV bytes can halve while total VRAM falls by less.", "Token equality on a small suite is a regression check, not a quality proof."],
        ["Predict whether the RTX 5090 build accepts FP8 KV.", "Compare theoretical bytes per token.", "Choose rollback evidence for a long-context route."],
        "Run matched native generations with BF16/auto and FP8 KV configurations, retaining success, tokens, and timing.",
        "automatic KV dtype", "FP8 KV dtype with the same BF16 weights", "model, prompts, greedy sampling, maximum length, GPU, and engine version", "initialization success, output token equality, elapsed time, and theoretical KV byte ratio",
        "The code destroys the first engine before creating the second and records configuration failures verbatim. It avoids presenting a failed initialization as a performance measurement.",
        "Adopt FP8 KV only when native long-context capacity rises, task slices pass, and TTFT/ITL do not violate gates.",
        "Short prompts barely exercise cache capacity. Missing calibration scales or a different backend can alter accuracy and speed, while allocator reserves prevent exact 2× concurrency.",
        "Calibrate scales on representative contexts, sweep lengths to the admission limit, compare output distributions, and inspect cache-block capacity plus engine metrics.",
        "FP8 can halve the leading KV payload; the native A/B establishes only this model/build's execution and small-suite token behavior.",
        "native-backend",
        [("Auto success", "auto.success", "bool"), ("FP8 success", "fp8.success", "bool"), ("Theoretical KV ratio", "theoretical_kv_capacity_ratio", "ratio"), ("Token sequences equal", "token_sequences_equal", "bool"), ("Auto elapsed", "auto.elapsed_s", "float"), ("FP8 elapsed", "fp8.elapsed_s", "float")],
        ["kv_quant", "engine_args"],
        "flowchart LR\n  K[\"BF16 KV vectors\"] --> Q[\"scale + FP8 encode\"]\n  Q --> C[\"smaller cache blocks\"]\n  C --> A[\"attention read/dequantize\"]\n  A --> O[\"logits and token regression\"]\n  C --> M[\"long-context capacity test\"]",
        [("Separate weight and cache dtypes", "Keep model weights fixed during the comparison."), ("Account for scales", "Record how FP8 values are calibrated or dynamically scaled."), ("Test native execution", "Retain initialization, token, latency, and memory evidence."), ("Sweep long contexts", "Capacity value appears only when KV is a material budget term.")],
    ),
    L(
        16, "lora-serving", "Serving LoRA Adapters",
        "Can one base model safely serve many task adapters without duplicating all weights?",
        "LoRA keeps a shared base checkpoint and applies small low-rank deltas per request. The memory advantage is attractive, but adapter identity, rank limits, tokenizer compatibility, scheduling, and dynamic-load security become service concerns.",
        "The lab probes LoRA API and CLI support, builds a transparent low-rank memory ledger for the local architecture, and validates request-routing identities. It does not fabricate a trained adapter.",
        "For a matrix `W`, LoRA adds `ΔW = B A` with rank `r`; storage scales with `r(in+out)` rather than `in×out`. vLLM can batch requests associated with different adapters while sharing the base weights, subject to configured rank and resident-adapter limits. The adapter path and name become executable inputs.",
        ["LoRA shares base weights but adds per-adapter state.", "Adapter name and immutable revision belong in the request contract.", "Dynamic loading expands the filesystem and authorization boundary."],
        ["Estimate one adapter's bytes at rank 16.", "Probe `--enable-lora` and rank-related arguments.", "Explain why this lab cannot claim adapter output correctness."],
        "Calculate adapter storage and inspect the installed LoRA request/config surface with explicit missing-native evidence.",
        "full base-model duplication per task", "one base model plus rank-16 adapter deltas", "model geometry, dtype, target modules, rank, adapter count, and installed vLLM", "estimated bytes, compression ratio, API symbols, CLI flags, and native-adapter execution status",
        "The code derives matrix dimensions from config and counts only declared target projections. It records theoretical bytes separately from package/API availability.",
        "Enable multi-adapter serving only after signed adapter artifacts, task quality, isolation, load/unload, concurrency, and rollback tests pass.",
        "Real PEFT checkpoints contain configuration and may target a different module set. Runtime memory includes buffers, and an unauthorized local path can expose arbitrary artifacts.",
        "Create or obtain a versioned adapter, hash it, run baseline/adapter requests through `LoRARequest`, and stress simultaneous adapter residency and eviction.",
        "The low-rank ledger explains why adapters are small; native behavioral and performance claims remain pending without a real adapter artifact.",
        "compatibility-probe",
        [("Estimated adapter bytes", "estimated_adapter_bytes", "bytes"), ("Base weight bytes", "base_weight_bytes", "bytes"), ("Storage ratio", "adapter_to_base_ratio", "percent"), ("LoRA request API", "api.lora_request", "bool"), ("Enable flag", "cli.enable_lora", "bool"), ("Native adapter executed", "native_adapter_executed", "bool")],
        ["lora", "engine_args"],
        "flowchart LR\n  B[\"shared base weights W\"] --> Y[\"linear output\"]\n  R[\"request adapter ID\"] --> A[\"load A and B factors\"]\n  X[\"activation x\"] --> Y\n  A --> D[\"x B A low-rank delta\"]\n  D --> Y\n  Y --> O[\"adapter-specific result\"]",
        [("Freeze the base", "All tenants reference one immutable base revision."), ("Resolve an authorized adapter", "Map the request name to a signed local artifact."), ("Apply the low-rank delta", "Schedule adapter-specific factors beside shared weights."), ("Test isolation", "Verify quality, residency limits, eviction, and authorization.")],
    ),
    L(
        17, "speculative-decoding", "Speculative Decoding and Acceptance",
        "When does proposing several tokens reduce ITL instead of adding verification overhead?",
        "Speculative decoding accelerates memory-bound Decode only when cheap proposed tokens are accepted often enough. A method name or proposal length cannot predict the result without acceptance and target-model timing.",
        "The native lab compares ordinary decoding with prompt-lookup n-gram speculation on a repetitive prompt, using identical greedy sampling. It records success, elapsed time, token equality, and exposed acceptance metrics.",
        "A proposer emits multiple candidate tokens. The target verifies them in a batched pass and accepts the valid prefix; rejected positions resume target decoding. N-gram lookup proposes repeated prompt continuations without a draft model. Expected benefit depends on proposal cost, verification efficiency, acceptance length, and offered load.",
        ["Speculation changes execution, not the target distribution contract.", "Acceptance rate is workload-dependent.", "High-QPS batching can reduce the relative value of speculative Decode."],
        ["Predict acceptance on a repeated sequence prompt.", "Check greedy token equality between configurations.", "State why one elapsed-time sample is insufficient for promotion."],
        "Run matched native baseline and n-gram speculative engines, retaining token and timing evidence.",
        "ordinary target-model Decode", "n-gram prompt-lookup speculation with four proposed tokens", "model, prompt, greedy sampling, maximum tokens, engine limits, and GPU", "success, elapsed time, output tokens, token equality, and acceptance counters when exposed",
        "The two engines are created sequentially to avoid shared VRAM. The speculative config follows the installed release schema and any incompatibility is kept as a structured failure.",
        "Enable speculation only when representative low/medium-QPS traffic improves ITL without quality, throughput, or memory regressions.",
        "A repetitive prompt favors n-gram lookup and is not representative. Compilation warm-up, batching, and version-specific metrics can dominate a small run.",
        "Benchmark several prompt families and arrival rates, collect proposer/acceptance counters, and compare p50/p95 ITL after warm-up.",
        "Speculation is valuable only when accepted target work offsets proposer and verification cost; this native pair bounds the claim to one repetitive workload.",
        "native-backend",
        [("Baseline success", "baseline.success", "bool"), ("Speculative success", "speculative.success", "bool"), ("Tokens equal", "tokens_equal", "bool"), ("Baseline elapsed", "baseline.elapsed_s", "float"), ("Speculative elapsed", "speculative.elapsed_s", "float"), ("Speed ratio", "speed_ratio", "ratio"), ("Output tokens", "speculative.output_tokens", "int")],
        ["spec", "metrics"],
        "flowchart LR\n  C[\"current context\"] --> P[\"cheap proposer: k tokens\"]\n  P --> V[\"target verifies candidates\"]\n  V --> A{\"accepted prefix\"}\n  A -->|\"many accepted\"| F[\"advance several positions\"]\n  A -->|\"early rejection\"| R[\"resume target decode\"]\n  F --> C\n  R --> C",
        [("Select a proposer", "Match draft, n-gram, suffix, or MTP to available artifacts."), ("Verify with the target", "Acceptance preserves the target distribution contract."), ("Measure accepted progress", "Count how many target positions advance per verification."), ("Sweep offered load", "Compare ITL and throughput where Decode is actually the bottleneck.")],
    ),
    L(
        18, "structured-outputs-tools", "Structured Outputs and Tool Contracts",
        "Is valid JSON enough when an application requires a specific schema?",
        "Applications need typed fields, enums, and required properties, not text that merely resembles JSON. Constrained decoding moves part of that contract into token selection, while application validation still owns semantics and side effects.",
        "The lab runs native JSON-schema-constrained generation through SamplingParams, parses the text, validates required fields/types locally, and compares it with an unconstrained control.",
        "A structured-output backend masks tokens that would make the partial output invalid under a grammar or schema. This reduces syntax retries but does not verify that a tool exists, arguments are safe, or values are factually correct. Tool calling adds parser and chat-template requirements before any executor should act.",
        ["JSON syntax and schema conformance are different gates.", "Constrained decoding cannot validate real-world semantics.", "Tool execution must remain downstream of authorization and validation."],
        ["Predict whether the unconstrained control parses.", "Check every required field and enum.", "Name the boundary between generation and tool execution."],
        "Generate one schema-constrained object natively, parse it, and apply an independent validator.",
        "ordinary text generation prompted to return JSON", "native structured output constrained by a JSON schema", "model, schema, prompt, sampling, maximum tokens, and GPU", "native success, JSON parse, schema validation, required fields, and control parse",
        "The schema is embedded in the artifact and validation does not trust the model's claim. No external function is executed by the notebook.",
        "Allow structured output into an application only after schema, semantic, authorization, timeout, and side-effect controls pass.",
        "Backend/schema features change across vLLM versions, and a small model may produce schema-valid but useless values. JSON parsing alone misses enum and range constraints.",
        "Add nested schemas, streaming partials, tool-choice policies, adversarial prompts, retries, and a sandboxed mock executor with an audit log.",
        "Constrained decoding can establish syntax/schema form; application meaning and tool safety remain independent responsibilities.",
        "native-backend",
        [("Structured success", "structured.success", "bool"), ("JSON parsed", "structured.json_parsed", "bool"), ("Schema valid", "structured.schema_valid", "bool"), ("Required fields", "structured.required_fields_present", "bool"), ("Control JSON parsed", "control.json_parsed", "bool"), ("Output tokens", "structured.output_tokens", "int")],
        ["structured", "openai"],
        "flowchart LR\n  S[\"JSON schema / grammar\"] --> M[\"allowed-token mask\"]\n  L[\"model logits\"] --> M\n  M --> G[\"generated JSON text\"]\n  G --> V[\"independent schema validation\"]\n  V --> A[\"authorization + semantic checks\"]\n  A --> T[\"optional tool execution\"]",
        [("Define the contract", "Write required fields, types, enums, and ranges."), ("Constrain tokens", "Mask continuations that violate the grammar."), ("Validate independently", "Parse and apply the same schema outside generation."), ("Authorize side effects", "Never equate a valid argument object with permission to execute.")],
    ),
    L(
        19, "multimodal-pooling-rerank", "Multimodal, Embedding, and Rerank Service Boundaries",
        "Should one endpoint expose generation, image inputs, embeddings, and reranking for every model?",
        "vLLM supports multiple task families, but capability belongs to a model plus configuration—not to the server binary in general. Routing an unsupported task can fail late or produce a contract mismatch.",
        "The compatibility probe inspects the local model's architecture and vLLM task/model interfaces, maps requested endpoints to required capabilities, and records that no multimodal or pooling benchmark was run with this text-generation checkpoint.",
        "Generative models return token sequences; pooling models return embeddings or scores; multimodal models add processors and media payloads. Rerank endpoints require a scoring task and input pair schema. Each family changes batching dimensions, memory, security, and evaluation metrics. Capability discovery should gate route registration.",
        ["Server capability is the intersection of engine and model support.", "Pooling quality uses retrieval/ranking metrics rather than generation tokens.", "Remote media inputs expand the network and parser attack surface."],
        ["Classify the local Qwen checkpoint's primary task.", "Mark Chat, embeddings, rerank, and image routes ready or blocked.", "Name the dataset required to evaluate each enabled task."],
        "Build a capability matrix from local config and installed interfaces, preserving unsupported routes as explicit blocks.",
        "register every endpoint because vLLM exposes it", "register only model-capability routes with task-specific gates", "local checkpoint, installed vLLM, no substitute models, and declared endpoint requirements", "architecture, multimodal metadata, pooling indicators, route readiness, and missing test artifacts",
        "The code does not call an unsupported endpoint merely to manufacture an error. It derives a conservative matrix and makes every missing model/evaluation artifact visible.",
        "Publish a route only when the selected model natively executes it and passes task-specific quality, latency, and security tests.",
        "Architecture names alone can be ambiguous, and vLLM may infer tasks dynamically. The conservative probe can produce false negatives until a native model initialization confirms support.",
        "Add one pinned embedding, rerank, and multimodal checkpoint, then run retrieval NDCG/recall, pairwise ranking, image validation, and mixed-batch memory tests.",
        "A vLLM installation is multi-capability; this checkpoint is not. Route registration must follow native model/task evidence.",
        "compatibility-probe",
        [("Architecture", "model.architecture", "str"), ("Text generation ready", "routes.chat.ready", "bool"), ("Embeddings ready", "routes.embeddings.ready", "bool"), ("Rerank ready", "routes.rerank.ready", "bool"), ("Multimodal ready", "routes.multimodal.ready", "bool"), ("Native non-generation tests", "native_non_generation_tests", "int")],
        ["models", "openai"],
        "flowchart TD\n  M[\"model architecture + task\"] --> C{\"capability discovery\"}\n  C --> G[\"generation routes\"]\n  C --> E[\"embedding routes\"]\n  C --> R[\"rerank routes\"]\n  C --> V[\"multimodal routes\"]\n  G --> Q[\"task-specific quality + SLO gate\"]\n  E --> Q\n  R --> Q\n  V --> Q",
        [("Identify the task", "Read model architecture and native task support."), ("Register only valid routes", "Do not expose endpoints the model cannot execute."), ("Use task metrics", "Generation, retrieval, ranking, and vision need different evaluations."), ("Review input security", "Media and remote URLs require additional controls.")],
    ),
    L(
        20, "benchmarking-workloads", "Benchmarking Latency, Throughput, and Workloads",
        "Which benchmark result survives a change in prompt length, output length, or concurrency?",
        "A single tokens-per-second value collapses the workload. A useful benchmark retains request shapes, warm-up state, raw samples, successful token counts, and the distinction between offline throughput and online latency.",
        "The native experiment warms one engine and runs batch sizes 1, 4, and 8 across fixed prompts. It retains every wall-time sample and reports useful output throughput plus request throughput.",
        "Offline throughput measures completed work over a closed batch; online serving adds arrivals, queueing, TTFT, ITL, cancellation, and network. Larger batches can raise GPU utilization while increasing a request's wait. Prompt and output tokens must remain separate because they exercise Prefill and Decode differently.",
        ["Workload identity includes both input and output lengths.", "Raw samples make percentile and noise checks recomputable.", "Throughput and tail latency can move in opposite directions."],
        ["Predict which batch has the highest output tokens/s.", "Check whether all requests generated the same token limit.", "State why this is not a concurrency benchmark."],
        "Warm one native engine and measure repeated closed batches at three batch sizes.",
        "batch size one", "batch sizes four and eight", "model, prompt template, maximum output, greedy sampling, warm-up, repeats, and GPU", "raw elapsed samples, prompt/output tokens, request/s, output tokens/s, and memory",
        "The engine stays alive across the sweep. Each batch uses distinct prompts of similar length, and the artifact stores samples rather than only a rounded mean.",
        "Use a benchmark row only for the workload cell it names; production promotion additionally requires open-loop service replay and tail gates.",
        "Closed-loop batches create no queue and may benefit from cache/compilation warm-up. Small sample counts and generated-length variation can bias rates.",
        "Use `vllm bench serve` or a timestamped client to sweep request rate, concurrency, prompt/output distributions, streaming, and SLO attainment.",
        "The native sweep establishes offline batching behavior for three frozen cells; it cannot be generalized to online traffic without new evidence.",
        "native-backend",
        [("Batch-1 output tok/s", "batches.1.output_tokens_s", "rate"), ("Batch-4 output tok/s", "batches.4.output_tokens_s", "rate"), ("Batch-8 output tok/s", "batches.8.output_tokens_s", "rate"), ("Batch-1 requests/s", "batches.1.requests_s", "rate"), ("Batch-8 requests/s", "batches.8.requests_s", "rate"), ("Peak allocated", "peak_allocated_mib", "mib")],
        ["bench", "metrics"],
        "flowchart LR\n  W[\"prompt/output distribution\"] --> B[\"benchmark cell\"]\n  C[\"batch or arrival pattern\"] --> B\n  E[\"engine + model + GPU\"] --> B\n  B --> R[\"raw per-request/per-run records\"]\n  R --> T[\"TTFT + ITL + latency + throughput\"]\n  T --> G[\"SLO attainment, not one headline number\"]",
        [("Freeze the workload", "Record input/output tokens and arrival or batch policy."), ("Warm intentionally", "Separate cold compilation/startup from steady state."), ("Keep raw records", "Retain samples and successful token counts."), ("Report the right scope", "Do not turn an offline batch rate into an online SLO.")],
    ),
    L(
        21, "production-metrics", "Production Metrics and Alertable Signals",
        "Which metric tells you that users are waiting even while GPU utilization looks healthy?",
        "GPU utilization can remain high while the waiting queue, preemption, cache pressure, or TTFT deteriorates. Operations need request-, scheduler-, cache-, and process-level signals with labels that do not explode cardinality.",
        "The lab launches a real localhost vLLM server, generates traffic, scrapes `/metrics`, parses Prometheus samples, and verifies a small required-signal set. It retains names and selected values, not an unbounded scrape.",
        "Counters accumulate events and should be converted to rates; gauges represent current queue/cache state; histograms support latency distributions over time. Alerts should connect a symptom such as high TTFT to demand, running/waiting requests, cache usage, errors, and saturation. Request IDs and prompts belong in traces/logs under data policy, not metric labels.",
        ["A healthy process is not a healthy SLO.", "Metric type determines the correct query.", "Low-cardinality labels are a production requirement."],
        ["Predict which metric families appear after one request.", "Distinguish counter, gauge, and histogram usage.", "Write one multi-signal queueing alert."],
        "Serve the local model, issue traffic, scrape Prometheus exposition, and validate required metric families.",
        "GPU utilization alone", "request, scheduler, cache, latency, and error signals", "same server/model, loopback client, one request, scrape time, and parser", "HTTP status, metric family count, required names, selected values, and unsafe-label scan",
        "The parser ignores comments and keeps only finite numeric samples. It scans label names for obvious request-content fields and stores a bounded name list for review.",
        "Create an alert only when its metric semantics, window, traffic threshold, runbook, and false-positive behavior are tested.",
        "One scrape cannot calculate a rate or quantile and some metrics remain zero without concurrent load. Metric names can change across releases.",
        "Replay sustained and overload traffic, evaluate recording rules, test alerts, and correlate client TTFT with engine histograms and logs.",
        "The native scrape proves observability wiring and available signal names; alert thresholds require time-series workload evidence.",
        "native-backend",
        [("Metrics status", "metrics_status", "int"), ("Metric families", "metric_families", "int"), ("Required present", "required_present", "int"), ("Required total", "required_total", "int"), ("Unsafe labels", "unsafe_label_hits", "int"), ("Request succeeded", "request_succeeded", "bool")],
        ["metrics", "prometheus"],
        "flowchart LR\n  U[\"client SLO symptoms\"] --> H[\"latency histograms\"]\n  Q[\"running + waiting requests\"] --> H\n  K[\"KV cache usage\"] --> H\n  E[\"errors + preemptions\"] --> H\n  H --> A[\"multi-signal alert\"]\n  A --> R[\"runbook and rollback\"]",
        [("Start from the SLO", "Choose user-visible TTFT, ITL, completion, and error indicators."), ("Add causes", "Observe queue, cache, preemption, and process saturation."), ("Respect metric types", "Rate counters and aggregate histograms over windows."), ("Test the alert", "Drive known failure states and follow the runbook.")],
    ),
    L(
        22, "docker-deployment", "A Reproducible Single-Node Container",
        "What must a container specification pin beyond the vLLM image tag?",
        "A container packages user space, but it still depends on host driver/runtime integration, GPU visibility, shared memory, model/cache mounts, secrets, health probes, and a rollback image digest.",
        "The configuration audit builds a Docker deployment manifest, validates digest pinning, read-only model mounts, cache separation, IPC/shared-memory choice, secret handling, health checks, and resource limits. Docker execution is explicitly absent on the remote training container.",
        "The NVIDIA container runtime passes devices and driver libraries into an image. vLLM may use shared memory for tensor-parallel communication; model caches should persist outside the writable layer. Immutable image digests and model hashes allow rollback, while API keys should enter through a secret mechanism rather than command arguments.",
        ["The image does not contain the host GPU driver.", "A floating tag is not an immutable release identity.", "Writable cache, model, logs, and secrets have different lifecycle rules."],
        ["Find every mutable identifier in the draft spec.", "Check that model files are mounted read-only.", "Name the host-level test still required."],
        "Generate and lint a single-node container manifest against twelve deployment invariants.",
        "an unpinned `latest` image and implicit volumes", "digest-pinned image, explicit GPU/runtime, mounts, health, secrets, and rollback", "one model manifest, serving arguments, port, and security policy", "passed checks, failed checks, image pinning, mount modes, health command, and native Docker status",
        "The notebook represents the deployment as data and evaluates named checks. It does not call Docker when the daemon is outside the environment, so no container-runtime success is implied.",
        "Promote the container spec only after lint gates and a cold host start complete generation, health, restart, and rollback tests.",
        "Static lint cannot verify host driver compatibility, pull permissions, runtime hooks, actual shared-memory needs, or cold-start duration.",
        "Run the digest on a clean GPU host, verify model hashes, send traffic, restart during load, rotate the secret, and roll back to the previous digest.",
        "The audited manifest closes common reproducibility and secret-handling gaps; actual container execution remains a separate host test.",
        "compatibility-probe",
        [("Checks passed", "checks_passed", "int"), ("Checks total", "checks_total", "int"), ("Image digest pinned", "image_digest_pinned", "bool"), ("Model read-only", "model_read_only", "bool"), ("Secret external", "secret_external", "bool"), ("Native Docker executed", "native_docker_executed", "bool")],
        ["docker", "security"],
        "flowchart LR\n  I[\"image digest\"] --> C[\"container spec\"]\n  M[\"model hash + read-only mount\"] --> C\n  S[\"secret injection\"] --> C\n  G[\"GPU runtime + shared memory\"] --> C\n  C --> H[\"health + generation test\"]\n  H --> R[\"promote or rollback digest\"]",
        [("Pin immutable inputs", "Use image digest, model hash, and explicit arguments."), ("Separate mounts", "Make model read-only and cache/log destinations intentional."), ("Inject runtime concerns", "Configure GPU access, shared memory, ports, and secrets."), ("Test on a clean host", "Exercise health, generation, restart, and rollback.")],
    ),
    L(
        23, "kubernetes-gpu-rollout", "Kubernetes GPU Scheduling and Rollouts",
        "Can a Deployment be highly available when every replica needs a scarce GPU and 30 GB of model state?",
        "Kubernetes can restart processes and place pods, but it cannot create GPU capacity, shorten model loading, or make a single replica redundant. Requests, topology, probes, disruption budgets, and rollout surge must be designed around the inference lifecycle.",
        "The lab renders and parses a minimal Deployment/Service/PDB configuration, then checks GPU requests/limits, rolling-update feasibility, readiness/startup probes, termination grace, cache strategy, and anti-affinity. No cluster is claimed.",
        "Device plugins advertise GPU resources and the scheduler treats them as indivisible. Readiness should wait for a loaded model; startup probes protect slow initialization; preStop and grace periods drain traffic. `maxSurge` requires spare GPU capacity, while `maxUnavailable` trades availability for an in-place rollout.",
        ["GPU resource requests are placement contracts.", "Liveness must not kill a healthy model during slow startup.", "A zero-downtime surge needs an actually free GPU."],
        ["Calculate GPUs needed during a two-replica maxSurge rollout.", "Check probe roles and grace period.", "Choose the rollback signal before applying the manifest."],
        "Validate a GPU Deployment, Service, and disruption policy against rollout and lifecycle invariants.",
        "generic CPU-style Deployment defaults", "GPU-aware resources, probes, drain, topology, and rollout budget", "replica count, one GPU/pod, model load time, and declared cluster capacity", "manifest checks, steady GPUs, surge GPUs, capacity feasibility, probe presence, and native-cluster status",
        "The YAML is embedded and parsed into ordinary dictionaries. Each warning names the missing operational consequence rather than merely failing schema syntax.",
        "Apply only when steady and rollout GPU capacity, probes, drain, disruption, metrics, and rollback revision are all demonstrated in staging.",
        "Static configuration cannot verify device plugin health, image/model pull time, scheduler fragmentation, node failures, or actual probe behavior.",
        "Deploy to a staging cluster, delete pods/nodes during load, run a canary, test rollout with saturated GPUs, and measure time to readiness and traffic drain.",
        "The manifest audit establishes scheduling and rollout intent; Kubernetes availability remains unmeasured until a real cluster test.",
        "compatibility-probe",
        [("Checks passed", "checks_passed", "int"), ("Checks total", "checks_total", "int"), ("Steady GPUs", "capacity.steady_gpus", "int"), ("Rollout GPUs", "capacity.rollout_gpus", "int"), ("Capacity feasible", "capacity.feasible", "bool"), ("Startup probe", "startup_probe", "bool"), ("Native cluster executed", "native_cluster_executed", "bool")],
        ["kubernetes", "k8s"],
        "flowchart TD\n  D[\"Deployment revision\"] --> S[\"scheduler: GPU + topology\"]\n  S --> P[\"pod starts and loads model\"]\n  P --> R{\"readiness passes?\"}\n  R -->|\"yes\"| T[\"receive traffic\"]\n  R -->|\"no\"| W[\"stay out of Service\"]\n  T --> G[\"drain on termination\"]\n  G --> O[\"old pod removed\"]",
        [("Request the device", "Declare one GPU resource per serving pod."), ("Protect initialization", "Use startup and readiness probes with realistic model-load windows."), ("Budget the rollout", "Ensure surge capacity exists or accept controlled unavailability."), ("Drain and verify", "Stop new traffic, finish requests, and retain rollback evidence.")],
    ),
    L(
        24, "gateway-multi-tenant", "Gateway Admission, Rate Limits, and Multi-Tenancy",
        "Should one cheap short request and one 32K batch job spend the same rate-limit unit?",
        "Request-count limits ignore prompt and output work. In a shared GPU service, one tenant can fill the queue or KV cache with a few large jobs while remaining under requests per minute.",
        "A deterministic gateway simulation compares request-count and token-budget admission for interactive and batch tenants. It retains admitted work, rejects, per-tenant waiting, and fairness indices.",
        "A token-bucket can charge prompt tokens immediately and reserve an output allowance, then reconcile actual usage. Separate service classes and concurrency caps prevent large batch traffic from occupying every active slot. Authentication establishes tenant identity; authorization maps it to models, adapters, budgets, and logging policy.",
        ["Admission cost should approximate scarce resources.", "Rate, concurrency, and queue caps solve different abuse modes.", "Tenant identity must survive through metrics and audit without leaking secrets."],
        ["Predict which policy admits more oversized batch work.", "Choose a safe output reservation rule.", "Define one fairness and one SLO gate."],
        "Replay two tenant workloads through request-count and token-budget gateways.",
        "equal request-count buckets", "prompt/output token budgets plus concurrency and service classes", "same arrivals, token estimates, engine capacity, and tenant weights", "admitted requests/tokens, rejection reasons, p95 wait, class isolation, and fairness",
        "The simulator keeps admission separate from GPU scheduling and records every decision. Its token costs are declared estimates rather than secret model calculations.",
        "Adopt a policy only when premium SLOs, batch throughput, fairness, abuse resistance, and usage reconciliation meet written gates.",
        "Clients can understate output demand, tokenization varies by model, and retries can amplify load. A numerical queue omits cache and real scheduler interactions.",
        "Place the gateway before a staging vLLM pool, replay signed multi-tenant traffic, cancel requests, exhaust quotas, and reconcile server usage with billing records.",
        "Token-aware admission better represents inference cost than request counts, but production limits require native traffic calibration.",
        "numerical-model",
        [("Count-policy admitted tokens", "request_count.admitted_tokens", "int"), ("Token-policy admitted tokens", "token_budget.admitted_tokens", "int"), ("Count-policy batch admits", "request_count.batch_admitted", "int"), ("Token-policy batch admits", "token_budget.batch_admitted", "int"), ("Token-policy interactive p95", "token_budget.interactive_p95_wait", "float"), ("Token-policy fairness", "token_budget.fairness", "float")],
        ["openai", "metrics"],
        "flowchart LR\n  A[\"authenticated request\"] --> P[\"prompt token charge\"]\n  P --> O[\"reserve output allowance\"]\n  O --> C{\"quota + concurrency + route allowed?\"}\n  C -->|\"yes\"| Q[\"service-class queue\"]\n  C -->|\"no\"| R[\"bounded rejection\"]\n  Q --> V[\"vLLM pool\"]\n  V --> U[\"reconcile actual usage\"]",
        [("Authenticate identity", "Bind a request to tenant, route, and policy."), ("Estimate resource cost", "Charge prompt work and reserve an output budget."), ("Apply layered limits", "Check rate, concurrent requests, queue depth, and service class."), ("Reconcile usage", "Return unused allowance and audit actual token counts.")],
    ),
    L(
        25, "reliability-debugging", "Diagnosing OOM, CUDA, and Tokenizer Failures",
        "Where should investigation start when one request returns 500 after a seemingly healthy deploy?",
        "Inference failures cross API validation, tokenizer, model/config, scheduler/cache, CUDA kernels, and host resources. Randomly changing memory utilization or reinstalling packages destroys evidence and can hide the first failing layer.",
        "The lab executes a five-layer diagnostic checklist against the real environment and intentionally evaluates safe invalid configurations without triggering a GPU OOM. It captures pass/fail, versions, free memory, model/tokenizer files, and bounded exception classes.",
        "Start at the earliest reproducible boundary: request schema and token count, model/tokenizer identity, engine configuration, GPU/driver state, then kernel/runtime logs. OOM investigation needs free/used/reserved memory, requested context/concurrency, and cache policy. CUDA errors can surface asynchronously, so the original operation and preceding logs matter.",
        ["Preserve the first error before retrying.", "Tokenizer/model drift can resemble a runtime regression.", "OOM is a budget violation, not a reason to blindly lower every limit."],
        ["Predict which safe invalid input fails before GPU execution.", "List the environment facts captured before diagnosis.", "Write a rollback condition for unexplained CUDA errors."],
        "Run a layered environment/config/tokenizer diagnostic and classify safe failure probes.",
        "unstructured trial-and-error", "ordered request→tokenizer→model→engine→GPU diagnosis", "current environment, local checkpoint, no destructive OOM, and captured exceptions", "layer checks, first failing layer, free memory, tokenization success, import/CUDA success, and safe-probe classifications",
        "The notebook never allocates to exhaustion. It uses schema and configuration checks to demonstrate localization while retaining the actual stack identity.",
        "Roll back when the first failure cannot be reproduced and explained within the canary window or when data corruption/non-deterministic CUDA errors appear.",
        "Safe probes do not reproduce fragmentation, NCCL failures, illegal memory access, or load-dependent queue bugs. Passing diagnostics is not a stress test.",
        "Replay the failing request with correlation IDs and debug logs in staging, then add targeted concurrency, context, cancellation, and fault-injection tests.",
        "Layered diagnosis preserves causality and shortens rollback decisions; this safe lab validates the checklist rather than inducing production failures.",
        "compatibility-probe",
        [("Checks passed", "checks_passed", "int"), ("Checks total", "checks_total", "int"), ("First failing layer", "first_failing_layer", "str"), ("CUDA available", "cuda_available", "bool"), ("Free GPU memory", "free_gpu_mib", "mib"), ("Tokenizer files", "tokenizer_files", "int"), ("Safe failures classified", "safe_failures_classified", "int")],
        ["install", "engine_args"],
        "flowchart TD\n  E[\"first captured error + request ID\"] --> A{\"API/schema valid?\"}\n  A -->|\"yes\"| T{\"tokenizer/model identity valid?\"}\n  T -->|\"yes\"| C{\"engine config and capacity valid?\"}\n  C -->|\"yes\"| G{\"GPU/driver/kernel healthy?\"}\n  G --> L[\"load/concurrency reproduction\"]\n  A --> R[\"fix or reject request\"]\n  T --> R\n  C --> R\n  G --> R",
        [("Freeze the failure", "Save the first request, error, versions, and resource state."), ("Walk from outside in", "Validate API, tokenization, model, engine, and GPU in order."), ("Test one hypothesis", "Change only the variable implied by the first failed layer."), ("Decide quickly", "Use a written canary deadline and rollback condition.")],
    ),
    L(
        26, "performance-tuning", "A Hypothesis-Driven Tuning Loop",
        "Which engine knob should change first when throughput is low but p95 is already close to the SLO?",
        "Tuning many flags at once produces an irreproducible winner. A disciplined loop starts from a bottleneck hypothesis, changes one factor, records raw evidence, and rejects improvements that violate another gate.",
        "The native lab holds one engine and prompt family constant while sweeping batch sizes 1, 2, 4, and 8. It calculates output throughput and a latency proxy, then applies a declared throughput/p95 decision rule.",
        "Batching can amortize weight reads and raise occupancy, but closed-batch completion latency grows with more work. Engine limits such as maximum sequences, maximum batched tokens, memory utilization, eager execution, and compilation interact with workload shape. A Pareto frontier is more useful than one scalar score.",
        ["One experiment should test one written hypothesis.", "Every candidate retains all acceptance metrics.", "A throughput gain outside the latency gate is not a winner."],
        ["Predict where throughput begins to saturate.", "Identify the held-constant variables.", "Apply the gate before reading the candidate name."],
        "Run a native batch-size sweep and select only Pareto/gate-feasible rows.",
        "batch size one", "batch sizes two, four, and eight under one engine", "model, prompts, token limit, sampling, warm-up, repeats, and GPU", "raw elapsed samples, output tokens/s, batch completion p95, memory, feasible set, and selected row",
        "The code stores raw samples for every cell and calculates the gate from data. It does not tune engine construction flags in the same experiment.",
        "Promote the smallest-complexity candidate that materially improves the target metric while every quality, latency, memory, and error gate passes.",
        "Closed batches do not measure TTFT/ITL or arrival queues. Batch-size effects can differ after compilation, prefix caching, quantization, or longer outputs.",
        "Choose the next single-variable hypothesis from a native profile, then repeat with open-loop service traffic and confidence intervals.",
        "A tuning result is a gated comparison with frozen variables and raw evidence, not a collection of unexplained flags.",
        "native-backend",
        [("Candidates", "candidates", "int"), ("Feasible candidates", "feasible_candidates", "int"), ("Selected batch", "selected.batch", "int"), ("Selected throughput", "selected.output_tokens_s", "rate"), ("Selected p95", "selected.p95_s", "float"), ("Peak allocated", "peak_allocated_mib", "mib")],
        ["bench", "metrics"],
        "flowchart LR\n  B[\"baseline evidence\"] --> H[\"one bottleneck hypothesis\"]\n  H --> C[\"change one variable\"]\n  C --> M[\"measure raw samples\"]\n  M --> G{\"all gates pass?\"}\n  G -->|\"yes\"| P[\"candidate on Pareto frontier\"]\n  G -->|\"no\"| R[\"reject and preserve baseline\"]\n  P --> H",
        [("Write the hypothesis", "Name the bottleneck and expected metric movement."), ("Freeze the comparison", "Change one engine or workload variable."), ("Retain the distribution", "Keep samples, resource data, and error counts."), ("Apply all gates", "Select from feasible Pareto rows and preserve rollback.")],
    ),
    L(
        27, "disaggregated-prefill-decode", "Disaggregated Prefill and Decode",
        "When does moving KV state between separate Prefill and Decode workers help tail latency?",
        "Prefill and Decode prefer different batch and compute characteristics. Separating them can isolate interference and scale phases independently, but KV transfer adds bandwidth, serialization, routing, and failure costs.",
        "The single-GPU lab probes installed KV-connector/NIXL interfaces and evaluates a capacity model across context sizes and link bandwidths. No two-worker native deployment is claimed.",
        "A Prefill worker creates KV bytes proportional to prompt tokens and model cache geometry. Before Decode can continue elsewhere, that state or a transferable representation must become available. Transfer time is approximately bytes divided by effective bandwidth plus coordination latency. Disaggregation helps only if saved queue/interference time exceeds that cost at the target reliability level.",
        ["KV transfer lies on the request's first-token path.", "Phase separation enables independent scaling but duplicates other resources.", "Connector availability is not a working two-node deployment."],
        ["Calculate KV transfer bytes for 8K BF16 context.", "Compare 25 and 200 Gb/s ideal transfer times.", "Write the native evidence required for a go decision."],
        "Probe connector vocabulary and compute transfer break-even rows from local model geometry.",
        "co-located Prefill/Decode with interference", "separate workers plus KV transfer", "model geometry, KV dtype, context lengths, bandwidth assumptions, and coordination overhead", "KV bytes, ideal transfer time, break-even saved delay, connector symbols, and native deployment status",
        "The model labels bandwidth as an assumption and never substitutes ideal link rate for measured application throughput. Connector imports are recorded independently.",
        "Disaggregate only when native end-to-end p95 improves after transfer, failure recovery, duplicate capacity, and operational cost are included.",
        "Compression, RDMA registration, topology, cache reuse, backpressure, failures, and scheduling can dominate ideal transfer arithmetic. One GPU cannot execute both roles independently.",
        "Deploy two workers with a supported connector, trace KV events, throttle the link, kill each role, and compare complete TTFT/ITL distributions with co-location.",
        "The break-even model identifies contexts and links worth testing; it is not evidence that disaggregated serving is faster.",
        "capacity-model",
        [("BF16 bytes/token", "kv_bytes_per_token", "bytes"), ("8K KV transfer", "contexts.8192.kv_mib", "mib"), ("8K at 25Gb/s", "contexts.8192.transfer_ms_25gbps", "float"), ("8K at 200Gb/s", "contexts.8192.transfer_ms_200gbps", "float"), ("Connector probe", "connector_probe", "bool"), ("Native disaggregation", "native_disaggregation_executed", "bool")],
        ["disagg", "metrics"],
        "flowchart LR\n  R[\"prompt request\"] --> P[\"Prefill worker\"]\n  P --> K[\"KV blocks\"]\n  K --> X[\"connector / network transfer\"]\n  X --> D[\"Decode worker\"]\n  D --> O[\"streamed tokens\"]\n  P -. \"phase capacity\" .-> S[\"independent scaling\"]\n  D -. \"phase capacity\" .-> S",
        [("Measure phase interference", "Establish the co-located TTFT/ITL problem first."), ("Account for KV bytes", "Derive transfer size from context and model geometry."), ("Test the connector", "Measure application bandwidth, coordination, and failures."), ("Compare complete systems", "Include duplicated resources and end-to-end tail latency.")],
    ),
    L(
        28, "capacity-cost-autoscaling", "Capacity, Cost, and Autoscaling",
        "How many replicas are required when average traffic is comfortable but the peak SLO is not?",
        "Capacity planning converts a measured service curve into replicas and headroom. Average tokens per second hides burstiness, prompt/decode mix, failure reserve, and the latency cliff near saturation.",
        "The lab anchors a queue/capacity model to a small native throughput measurement on the RTX 5090, then calculates replicas for three demand scenarios with utilization and N+1 reserve.",
        "Usable replica capacity is measured token throughput multiplied by a safe utilization target, not the saturation maximum. Required replicas are the ceiling of demand divided by usable capacity, then adjusted for availability and heterogeneous prompt cost. Autoscaling signals need lead time because new replicas load gigabytes of weights.",
        ["Saturation throughput is not an SLO-safe operating point.", "Headroom covers burst, variance, and failure—not just growth.", "Scale-up delay must be compared with traffic forecast horizon."],
        ["Compute replicas at 50%, 70%, and 85% utilization targets.", "Add N+1 reserve.", "Choose a pre-saturation scaling signal."],
        "Measure one native token rate and feed it into an explicit replica/autoscaling table.",
        "replicas from average demand and peak throughput", "SLO-safe utilization plus peak demand, N+1, and load delay", "measured model/GPU rate, demand scenarios, utilization target, and availability policy", "native rate, usable rate, base replicas, reserved replicas, utilization, and scale trigger",
        "The measurement and planning arithmetic stay in separate fields. A scenario never changes the measured RTX 5090 throughput value.",
        "Provision only after peak-trace replay confirms the chosen replica count meets TTFT/ITL/error gates with one replica unavailable.",
        "One offline rate omits online queueing and prompt work. Cost varies by provider, reservation, power, and utilization; this lesson does not quote a currency price.",
        "Build an online service curve, inject a replica failure at peak, measure startup time, and validate predictive or queue-based autoscaling before production.",
        "Replica arithmetic must be anchored to an SLO-safe service curve; the current native rate is a laboratory input, not production capacity.",
        "capacity-model",
        [("Measured output tok/s", "measured_output_tokens_s", "rate"), ("Safe utilization", "safe_utilization", "percent"), ("Usable tok/s", "usable_output_tokens_s", "rate"), ("Medium replicas", "scenarios.medium.with_reserve", "int"), ("Peak replicas", "scenarios.peak.with_reserve", "int"), ("Scale-up lead", "scale_up_lead_s", "float")],
        ["bench", "metrics"],
        "flowchart LR\n  M[\"measured service curve\"] --> U[\"safe utilization ceiling\"]\n  D[\"forecast peak demand\"] --> R[\"ceil demand / usable capacity\"]\n  U --> R\n  R --> N[\"N+1 / zone reserve\"]\n  L[\"model load lead time\"] --> A[\"autoscaling trigger\"]\n  N --> A",
        [("Measure a service curve", "Find throughput that still satisfies the latency SLO."), ("Choose operating headroom", "Reserve capacity for variance and recovery."), ("Calculate failure-aware replicas", "Add the declared availability reserve."), ("Trigger before saturation", "Account for image/model load and readiness delay.")],
    ),
    L(
        29, "security-compliance", "Security and Compliance Boundaries",
        "Can an authenticated generation request still reach private infrastructure or leak sensitive data?",
        "Authentication identifies a caller; it does not make remote media URLs, local model paths, custom code, prompts, logs, adapters, or generated tool arguments safe. Every input channel needs a trust and retention decision.",
        "The lab evaluates a URL allowlist/SSRF policy against public, loopback, link-local, private, malformed, and redirect-like cases; it also lints a release data-policy manifest for secrets, prompt logging, and model-license fields.",
        "SSRF defenses parse the URL, resolve all addresses, reject non-HTTP schemes and private/link-local/loopback ranges, revalidate redirects, and constrain size/content. Prompt and response data need collection purpose, encryption, retention, deletion, and access policy. Model licenses and `trust_remote_code` are supply-chain controls rather than request filters.",
        ["Authentication and input safety are independent layers.", "DNS/redirect revalidation is required after the initial string check.", "Observability must not silently become indefinite prompt storage."],
        ["Classify each URL fixture.", "Find which data-policy fields are missing.", "Write one release blocker for remote code or model license."],
        "Run deterministic SSRF-policy fixtures and lint a serving data/supply-chain manifest.",
        "accept authenticated URLs and log full requests", "allowlisted destinations, resolved-IP controls, bounded media, minimized logs, and provenance gates", "fixture URLs, simulated DNS map, manifest schema, no external fetch, and GPU identity", "allowed/blocked cases, false decisions, policy checks, retention days, and release blockers",
        "The URL test never performs network requests; a frozen DNS map makes the policy auditable and safe. The manifest linter names every missing control.",
        "Block release until input channels, secrets, remote code, model/license provenance, data retention, deletion, and incident ownership are approved and tested.",
        "A simulated resolver cannot expose DNS rebinding, proxy behavior, parser inconsistencies, decompression bombs, or real redirect chains. Compliance requirements are jurisdiction- and organization-specific.",
        "Test the gateway fetcher in an isolated network with redirect/rebinding fixtures, malware/media limits, audit access, deletion workflows, and legal review.",
        "Authenticated inference still needs strict input, supply-chain, and data-lifecycle controls; the lab verifies policy logic, not legal compliance.",
        "numerical-model",
        [("URL cases", "url_cases", "int"), ("URL decisions correct", "url_decisions_correct", "int"), ("Private blocked", "private_blocked", "bool"), ("Link-local blocked", "link_local_blocked", "bool"), ("Policy checks passed", "policy_checks_passed", "int"), ("Policy checks total", "policy_checks_total", "int"), ("Release blockers", "release_blockers", "int")],
        ["security", "openai"],
        "flowchart TD\n  R[\"authenticated request\"] --> I{\"input channel\"}\n  I --> U[\"URL parse + DNS/IP + redirect policy\"]\n  I --> P[\"prompt/data retention policy\"]\n  I --> A[\"adapter/model provenance\"]\n  U --> E[\"bounded engine request\"]\n  P --> E\n  A --> E\n  E --> L[\"minimized audit record\"]",
        [("Enumerate input channels", "Include URLs, files, prompts, adapters, schemas, and custom code."), ("Validate after resolution", "Reject unsafe schemes/addresses and re-check redirects."), ("Minimize data", "Collect only what has a purpose, retention, deletion, and access rule."), ("Gate the supply chain", "Pin model/adapter bytes, licenses, and executable trust.")],
    ),
    L(
        30, "production-launch", "From PoC to Canary: The Production Launch Gate",
        "What evidence must be true before a successful demo becomes a reversible service release?",
        "A production launch is a state transition backed by artifacts, not a meeting sentiment. Functional output, quality, performance, capacity, observability, security, failure recovery, and rollback must converge on one immutable release identity.",
        "The final notebook reads earlier Chapter 03 artifacts when available, verifies their hashes and required gates, creates a release manifest, and runs a deterministic PoC→load-test→canary→promotion state machine. Missing evidence blocks rather than defaults to pass.",
        "Each stage consumes evidence and has an exit criterion. PoC establishes native functionality and provenance; load testing establishes the service curve; canary compares SLO/error/quality slices; promotion requires monitoring and rollback readiness. A rollback trigger should be computable from live data, and the previous image/model/config tuple must remain deployable.",
        ["A release identity includes code, image, model, tokenizer, config, and environment.", "Every stage has explicit evidence and an owner.", "Rollback is tested before promotion, not designed during an incident."],
        ["Predict which missing artifact blocks the state machine.", "Check that each upstream evidence hash is retained.", "Write the exact canary rollback conditions."],
        "Aggregate selected executed artifacts into an immutable release gate and state-machine decision.",
        "a successful demo and manual approval", "evidence-complete PoC, load, canary, promotion, and rollback gates", "current repository artifacts, required lesson set, thresholds, release ID, and no invented passes", "artifact presence/hashes, functional gates, metrics gates, security gates, final stage, blockers, and rollback readiness",
        "The code reads only canonical JSON artifacts and computes hashes over their bytes. It refuses to infer a pass from prose or from a missing metric.",
        "Promote only when all required evidence passes and the exact previous release completes a tested rollback within its recovery objective.",
        "Lab artifacts come from one GPU and mostly synthetic traffic. A complete manifest can still be invalid for a different region, topology, model, demand distribution, or compliance scope.",
        "Run the manifest in staging and canary with production routing, inject failures, rehearse rollback, record the review owners, and repeat after any input hash changes.",
        "A reversible release is the conjunction of independent evidence gates; any missing required artifact correctly leaves the candidate blocked.",
        "capacity-model",
        [("Required artifacts", "required_artifacts", "int"), ("Artifacts present", "artifacts_present", "int"), ("Artifact hashes", "artifact_hashes", "int"), ("Gates passed", "gates_passed", "int"), ("Gates total", "gates_total", "int"), ("Final stage", "final_stage", "str"), ("Release ready", "release_ready", "bool"), ("Blockers", "blockers", "int")],
        ["metrics", "security"],
        "flowchart LR\n  P[\"PoC: function + provenance\"] --> L[\"load test: service curve\"]\n  L --> C[\"canary: live SLO + quality\"]\n  C --> G{\"all immutable gates pass?\"}\n  G -->|\"yes\"| R[\"promote with monitoring\"]\n  G -->|\"no\"| B[\"rollback exact prior release\"]\n  R --> M[\"post-launch review\"]\n  M --> P",
        [("Bind release identity", "Hash code, image, model, tokenizer, config, and evidence."), ("Advance by gates", "Require functional, load, security, and recovery proof per stage."), ("Canary with live comparators", "Evaluate SLO, errors, quality slices, and saturation."), ("Rollback mechanically", "Keep and rehearse the exact previous deployable tuple.")],
    ),
]
